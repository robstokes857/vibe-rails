// Code Atlas browser component. Generated by build-browser.mjs.
const rendererTemplate = "\u003c!doctype html>\r\n\u003chtml lang=\"en\">\r\n\u003chead>\r\n\u003cmeta charset=\"utf-8\">\r\n\u003cmeta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\r\n\u003cmeta name=\"color-scheme\" content=\"\u003c!-- ATLAS_MODE -->\">\r\n\u003cmeta http-equiv=\"Content-Security-Policy\" content=\"default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data:; font-src 'none'; connect-src 'none'; object-src 'none'; base-uri 'none'; form-action 'none'\">\r\n\u003ctitle>\u003c!-- ATLAS_TITLE -->\u003c/title>\r\n\u003cstyle>:root{--bg:var(--color-bg-base);--panel:var(--color-bg-surface);--surface:var(--color-bg-elevated);--line:var(--color-border);--text:var(--color-text);--muted:var(--color-text-muted);--mint:var(--color-primary);--blue:var(--color-accent);--violet:var(--color-accent);--amber:var(--color-warning);--radius:14px;font-family:Inter,\"Segoe UI\",Arial,sans-serif;font-synthesis:none}\r\n*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-size:14px}button,input,a{font:inherit}button,a,input{ -webkit-tap-highlight-color:transparent}button{color:inherit;cursor:pointer}button:disabled{cursor:default;opacity:.4}button:focus-visible,a:focus-visible,input:focus-visible,[tabindex]:focus-visible{outline:2px solid var(--mint);outline-offset:4px}button{border:0}a{color:var(--mint);text-decoration:none}button,input{touch-action:manipulation}[hidden]{display:none!important}button svg{pointer-events:none}::selection{background:var(--color-primary);color:var(--color-on-primary)}\r\n#atlas{min-height:100vh;display:grid;grid-template-columns:226px minmax(0,1fr);grid-template-rows:79px 1fr}.topbar{grid-column:1/-1;display:flex;align-items:center;border-bottom:1px solid var(--line);padding:0 30px;gap:32px;background:var(--color-bg-surface)}.status-light{display:inline-block;width:6px;height:6px;border-radius:50%;background:var(--mint);box-shadow:0 0 12px color-mix(in srgb, var(--color-primary) 46.67%, transparent);flex-shrink:0}.button{border-radius:7px;padding:10px 14px;font-size:12px;display:inline-flex;align-items:center;justify-content:center;gap:7px;transition:background .18s,transform .18s}.button:hover{transform:translateY(-1px)}.secondary{background:var(--color-bg-surface);border:1px solid var(--color-border);color:var(--color-text)}.secondary:hover{background:var(--color-bg-elevated)}.primary{background:var(--mint);color:var(--color-text-muted);font-weight:650;box-shadow:0 0 25px color-mix(in srgb, var(--color-primary) 3.92%, transparent)}.primary:hover{background:var(--color-primary)}.legend-title{font-size:9px;font-weight:600;color:var(--color-text-muted);letter-spacing:1.8px}.version{color:var(--color-text-muted)}.legend-title{margin-bottom:18px;font-size:8px}.dot{display:inline-block;border-radius:50%;width:5px;height:5px;margin-right:9px;vertical-align:middle}.mint{background:var(--mint)}.violet{background:var(--violet)}.blue{background:var(--blue)}.amber{background:var(--amber)}\r\n.main{min-width:0;padding:35px 34px 18px;display:flex;flex-direction:column}.page-heading{display:flex;align-items:center;justify-content:space-between;gap:20px;margin-bottom:27px}.eyebrow{font-size:9px;color:var(--color-text-muted);letter-spacing:2px;margin-bottom:13px;display:flex;gap:14px;align-items:center}h1{font-size:clamp(23px,2.45vw,35px);letter-spacing:-1.2px;line-height:1.16;margin:0;font-weight:500}h1>span{color:var(--mint)}.page-heading p{font-size:12px;color:var(--muted);margin:12px 0 0;line-height:1.6;max-width:620px}\r\n.map-shell{border:1px solid var(--line);border-radius:var(--radius);overflow:hidden;box-shadow:0 20px 80px color-mix(in srgb, var(--color-shadow) 13.33%, transparent);flex:1;display:flex;flex-direction:column;min-height:555px;background:var(--color-bg-base)}.crumb{color:var(--color-text);background:transparent;padding:8px 1px;font-size:11px}.crumb:last-child{color:var(--mint)}.search-wrap{position:relative;display:flex;align-items:center;background:var(--color-bg-base);border:1px solid var(--color-border);border-radius:7px;width:270px;flex-shrink:0}.search-icon{color:var(--color-text-muted);font-size:22px;padding:0 10px}#search{background:transparent;border:0;color:var(--color-text);min-width:0;width:100%;font-size:11px;padding:10px 0}#search:focus{outline:0}#search::placeholder{color:var(--color-text-muted)}.search-wrap:focus-within{border-color:var(--color-border)}.search-wrap kbd{margin:0 10px;border:1px solid var(--color-border);color:var(--color-text-muted);padding:1px 5px;border-radius:3px;font-size:9px}.search-results{position:absolute;right:0;top:43px;max-height:360px;overflow:auto;width:355px;max-width:82vw;border:1px solid var(--color-border);border-radius:10px;background:var(--color-bg-surface);box-shadow:0 20px 80px color-mix(in srgb, var(--color-shadow) 73.33%, transparent);padding:6px;z-index:40}.search-result{display:block;text-align:left;background:transparent;width:100%;border-radius:6px;padding:11px;color:var(--text)}.search-result:hover{background:var(--color-bg-elevated)}.search-result small{display:block;color:var(--color-text-muted);font-size:10px;margin-top:5px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.search-empty{color:var(--muted);padding:15px;font-size:12px}\r\n.map-body{display:flex;flex:1;position:relative;min-height:450px}.stage{flex:1;min-width:0;position:relative;overflow:hidden;isolation:isolate;background:radial-gradient(ellipse at 52% 53%,color-mix(in srgb, var(--color-bg-elevated) 33.33%, transparent) 0%,color-mix(in srgb, var(--color-bg-surface) 33.33%, transparent) 35%,var(--color-bg-base) 76%);cursor:grab;touch-action:none;min-height:465px}.stage.dragging{cursor:grabbing}.space-grid{position:absolute;inset:0;pointer-events:none;background-image:radial-gradient(color-mix(in srgb, var(--color-primary) 21.96%, transparent) .8px,transparent .8px);background-size:24px 24px;mask-image:radial-gradient(ellipse,#0009,transparent 80%)}.orbit{pointer-events:none;border:1px solid color-mix(in srgb, var(--color-border) 5.1%, transparent);border-radius:50%;position:absolute;left:50%;top:50%;transform:translate(-50%,-50%) rotate(-19deg);width:65%;height:65%}.orbit-two{width:92%;height:93%;transform:translate(-50%,-50%) rotate(19deg);border-style:dashed}.map-kicker{position:absolute;top:22px;left:23px;display:flex;align-items:center;gap:8px;font-size:8px;letter-spacing:1.6px;color:var(--color-text-muted);z-index:2;pointer-events:none}.map-kicker .status-light{width:4px;height:4px}.world{position:absolute;width:1200px;height:800px;transform-origin:0 0;will-change:transform}.world>svg{position:absolute;inset:0;width:1200px;height:800px;overflow:visible}#nodes{position:absolute;inset:0;pointer-events:none}.edge{fill:none;stroke:var(--graph-edge);stroke-opacity:.27;stroke-width:1.3;transition:stroke-opacity .25s}.edge-flow{fill:none;stroke:var(--graph-edge);stroke-width:1.6;stroke-dasharray:3 160;stroke-opacity:.5;animation:flow 9s linear infinite;pointer-events:none}.edge-hit{fill:none;stroke:transparent;stroke-width:14;cursor:help;pointer-events:stroke}.edge-group:hover .edge,.edge-group.highlight .edge{stroke-opacity:.95;stroke-width:2}.edge-group.dim{opacity:.15}.edge-group{transition:opacity .2s}.edge-label{fill:var(--color-text);font-size:10px;paint-order:stroke;stroke:var(--graph-edge);stroke-width:5px;stroke-linejoin:round;opacity:0;pointer-events:none}.edge-group:hover .edge-label,.edge-group.highlight .edge-label{opacity:1}.node{position:absolute;width:186px;min-height:93px;pointer-events:auto;text-align:left;border-radius:11px;background:linear-gradient(145deg,color-mix(in srgb, var(--color-bg-surface) 93.33%, transparent),color-mix(in srgb, var(--color-bg-surface) 96.08%, transparent) 78%);border:1px solid color-mix(in srgb, var(--color-border) 41.96%, transparent);color:var(--text);padding:15px;box-shadow:0 12px 25px color-mix(in srgb, var(--color-shadow) 26.67%, transparent),0 0 22px color-mix(in srgb, var(--color-accent) 3.92%, transparent);transition:border-color .22s,box-shadow .22s,opacity .22s;cursor:pointer;transform-origin:center;will-change:transform}.node:hover,.node:focus-visible{border-color:var(--node-color,var(--mint));box-shadow:0 14px 36px color-mix(in srgb, var(--color-shadow) 33.33%, transparent),0 0 32px color-mix(in srgb, var(--color-accent) 14.12%, transparent);z-index:4}.node.selected{border-color:var(--node-color,var(--mint));box-shadow:0 0 45px color-mix(in srgb, var(--color-accent) 12.55%, transparent);background:linear-gradient(145deg,var(--color-bg-elevated),var(--color-bg-surface));z-index:3}.node.dim{opacity:.22}.node-top{display:flex;align-items:center;gap:8px;font-size:8px;letter-spacing:1.2px;text-transform:uppercase;color:var(--node-color,var(--mint))}.node-symbol{width:20px;height:20px;border-radius:5px;background:color-mix(in srgb, var(--color-primary) 7.06%, transparent);display:grid;place-items:center;font-family:Consolas,monospace;font-size:12px;letter-spacing:-1px}.node-top .node-expand{margin-left:auto;color:var(--color-text-muted);font-size:13px}.node-name{display:block;font-size:12px;letter-spacing:-.15px;margin-top:9px;font-weight:550;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.node-meta{font-size:8px;color:var(--color-text-muted);margin-top:8px;display:flex;gap:7px;align-items:center}.node-meta span+span:before{content:'·';margin-right:7px}.root-node{width:154px;min-height:106px;text-align:center;background:radial-gradient(ellipse at 50% 0,color-mix(in srgb, var(--color-bg-elevated) 66.67%, transparent),var(--color-bg-surface));border:1px solid color-mix(in srgb, var(--color-border) 46.67%, transparent);border-radius:18px;box-shadow:0 0 65px color-mix(in srgb, var(--color-accent) 10.2%, transparent);padding:20px 12px}.root-node .node-top{justify-content:center;font-size:8px}.root-node .node-name{font-size:14px}.root-node .node-meta{justify-content:center}.root-glyph{font-size:20px;color:var(--mint);margin-bottom:7px;display:block}.empty-map{position:absolute;inset:0;display:grid;place-items:center;color:var(--muted);font-size:13px;pointer-events:none}\r\n.inspector{width:295px;flex-shrink:0;border-left:1px solid var(--color-border);background:var(--color-bg-surface);padding:22px;max-height:640px;overflow:auto;animation:slide-in .35s ease}.inspector-header{display:flex;align-items:center;justify-content:space-between;margin-bottom:23px;font-size:9px;letter-spacing:1.4px;color:var(--color-text-muted)}.icon-button{background:var(--color-bg-elevated);border:1px solid var(--color-border);border-radius:6px;width:27px;height:27px;color:var(--color-text);font-size:17px}.entity-kind{color:var(--mint);font-size:9px;text-transform:uppercase;letter-spacing:1.4px}.inspector h2{font-size:21px;letter-spacing:-.5px;font-weight:500;margin:12px 0;overflow-wrap:anywhere}.entity-path{font-family:Consolas,monospace;font-size:10px;color:var(--color-text-muted);overflow-wrap:anywhere;line-height:1.7}.entity-summary{font-size:12px;line-height:1.85;color:var(--color-text);margin:19px 0}.metric-grid{display:grid;grid-template-columns:1fr 1fr;gap:1px;border:1px solid var(--color-border);border-radius:8px;overflow:hidden;background:var(--color-bg-elevated);margin:22px 0}.metric{padding:14px 12px;background:var(--color-bg-surface)}.metric strong{display:block;font-size:22px;font-weight:400;color:var(--color-text);font-family:Consolas,monospace}.metric span{display:block;font-size:9px;color:var(--color-text-muted);margin-top:6px}.inspector .button{width:100%;margin-bottom:9px}.section-label{display:flex;justify-content:space-between;font-size:9px;letter-spacing:1.4px;color:var(--color-text-muted);margin:25px 0 11px;text-transform:uppercase}.relationship{display:flex;width:100%;align-items:center;gap:10px;text-align:left;background:transparent;border-bottom:1px solid var(--color-border);padding:11px 0;color:var(--color-text);font-size:11px}.relationship:hover{color:var(--mint)}.relationship>span:nth-child(2){min-width:0;flex:1;overflow-wrap:anywhere}.relationship small{font-size:8px;display:block;color:var(--color-text-muted);margin-top:5px}.relationship>span:first-child{color:var(--color-text-muted);font-size:15px}.relationship>span:last-child{font-size:12px;color:var(--color-text-muted)}.inspector .quiet,.quiet{font-size:11px;color:var(--color-text-muted);line-height:1.7}\r\ndialog{background:var(--color-bg-surface);color:var(--text);border:1px solid var(--color-border);border-radius:18px;padding:0;width:min(850px,92vw);max-height:86vh;box-shadow:0 30px 180px color-mix(in srgb, var(--color-shadow) 73.33%, transparent)}dialog::backdrop{background:color-mix(in srgb, var(--color-bg-surface) 74.12%, transparent);backdrop-filter:blur(10px)}dialog[open]{animation:modal-in .25s ease}#dialog-content{padding:30px}.dialog-top{display:flex;justify-content:space-between;align-items:flex-start;gap:20px}.dialog-top h2{font-weight:500;font-size:27px;margin:9px 0 6px;letter-spacing:-.7px;overflow-wrap:anywhere}.dialog-top .icon-button{flex-shrink:0}.detail-tabs{display:flex;gap:25px;border-bottom:1px solid var(--color-border);margin-top:27px}.detail-tab{background:transparent;color:var(--color-text-muted);padding:12px 0;border-bottom:2px solid transparent;font-size:12px}.detail-tab[aria-selected=true]{color:var(--mint);border-color:var(--mint)}.detail-panel{min-height:210px;padding-top:15px}.detail-panel .metric-grid{grid-template-columns:repeat(4,1fr);margin:9px 0 24px}.signature{background:var(--color-bg-base);border:1px solid var(--color-border);border-radius:8px;padding:17px;font-family:Consolas,monospace;font-size:12px;color:var(--color-text);overflow:auto;white-space:pre-wrap;line-height:1.7}.detail-list{display:grid;grid-template-columns:1fr 1fr;column-gap:30px}.history-entry{position:relative;margin-left:4px;padding:10px 0 22px 24px;border-left:1px solid var(--color-border)}.history-entry:before{content:'';position:absolute;left:-4px;top:16px;width:7px;height:7px;border-radius:50%;background:var(--color-primary)}.history-entry h3{font-weight:450;font-size:13px;margin:0 0 9px}.history-entry p{font-size:10px;color:var(--color-text-muted);margin:0}.history-entry code{color:var(--mint);font-size:10px;margin-right:13px}.changes-strip{display:flex;align-items:center;gap:18px;font-size:12px;color:var(--color-text-muted);padding:17px;border:1px solid var(--color-border);border-radius:8px;margin:0 0 20px}.additions{color:var(--color-text-muted)}.deletions{color:var(--color-danger)}.detail-links{display:flex;gap:10px;flex-wrap:wrap;margin-top:20px}.detail-links a{font-size:11px;border:1px solid var(--color-border);padding:8px 12px;border-radius:5px}.toast{position:fixed;bottom:27px;left:50%;transform:translateX(-50%);max-width:90vw;background:var(--color-bg-elevated);border:1px solid var(--color-border);color:var(--color-text);border-radius:9px;box-shadow:0 10px 40px color-mix(in srgb, var(--color-shadow) 53.33%, transparent);padding:14px 23px;font-size:12px;z-index:100}.toast.error{background:var(--color-bg-elevated);border-color:var(--color-danger);color:var(--color-danger)}\r\n@keyframes flow{to{stroke-dashoffset:-326}}@keyframes slide-in{from{opacity:0;transform:translateX(20px)}to{opacity:1;transform:translateX(0)}}@keyframes modal-in{from{opacity:0;transform:translateY(15px) scale(.97)}to{opacity:1;transform:translateY(0) scale(1)}}\r\n.node-name{font-size:14px}.node-meta{font-size:10px}.node-top{font-size:9px}.node.selected{width:232px;min-height:122px;padding:19px}.node.selected .node-name{font-size:17px}.node.selected .node-meta{margin-top:12px}.root-node .node-meta{font-size:8px}\r\n@media(min-width:1550px){.main{padding:43px 45px 24px}.page-heading{margin-bottom:36px}.stage{min-height:610px}.map-shell{min-height:710px}}\r\n@media(max-width:1150px){#atlas{grid-template-columns:185px minmax(0,1fr)}.topbar{padding:0 20px;gap:20px}.main{padding:27px 23px 16px}.inspector{width:250px;padding:19px}.search-wrap{width:220px}}\r\n@media(max-width:900px){#atlas{grid-template-columns:1fr;grid-template-rows:70px 1fr}.topbar{gap:15px}.page-heading{align-items:flex-start}.main{padding:24px 18px 14px}.map-body{flex-direction:column}.inspector{width:auto;max-height:380px;border-left:0;border-top:1px solid var(--color-border)}.stage{min-height:490px}.inspector .metric-grid{grid-template-columns:repeat(4,1fr)}.inspector .button{width:auto;margin-right:8px}.inspector-header{margin-bottom:10px}.inspector h2{margin:8px 0}.inspector .entity-summary{margin:10px 0}}\r\n@media(max-width:540px){.topbar{padding:0 15px}.main{padding:22px 12px 12px}.page-heading{display:block;margin-bottom:20px}.eyebrow{font-size:8px;letter-spacing:1.5px}h1{font-size:26px;letter-spacing:-.8px}.page-heading p{font-size:11px}.search-wrap{width:100%}.search-results{width:100%}.stage{min-height:450px}.map-kicker{font-size:7px;left:15px;top:18px}.map-shell{min-height:580px}#dialog-content{padding:22px}.detail-tabs{gap:22px}.detail-panel .metric-grid{grid-template-columns:1fr 1fr}.detail-list{grid-template-columns:1fr}.dialog-top h2{font-size:23px}.changes-strip{flex-wrap:wrap;gap:10px}.inspector .metric-grid{grid-template-columns:1fr 1fr}}\r\n@media(prefers-reduced-motion:reduce){*,*::before,*::after{animation:none!important;transition:none!important;scroll-behavior:auto!important}}\r\n\r\n/* v0.2:neutral charcoal chrome; color belongs to code entities. */\r\n:root{--bg:var(--color-bg-base);--panel:var(--color-bg-surface);--surface:var(--color-bg-elevated);--line:var(--color-border);--text:var(--color-text);--muted:var(--color-text-muted);--mint:var(--color-primary);--blue:var(--color-accent);--violet:var(--color-accent);--amber:var(--color-warning)}\r\n.topbar{background:var(--color-bg-surface)}.topbar{border-color:var(--color-border)}.status-light{background:var(--color-primary);box-shadow:0 0 12px color-mix(in srgb, var(--color-accent) 33.33%, transparent)}.legend-title{color:var(--color-text-muted)}.version{color:var(--color-text-muted)}.page-heading p{color:var(--color-text-muted)}.eyebrow{color:var(--color-text-muted)}.primary{background:var(--color-primary);color:var(--color-text-muted);box-shadow:none}.primary:hover{background:var(--color-primary)}.secondary{background:var(--color-bg-elevated);border-color:var(--color-border);color:var(--color-text)}.secondary:hover{background:var(--color-bg-elevated)}\r\n.main{padding-top:27px}.page-heading{margin-bottom:22px}h1{font-weight:450;font-size:31px}.map-shell{background:var(--color-bg-surface);border-color:var(--color-border);box-shadow:0 16px 50px color-mix(in srgb, var(--color-shadow) 13.33%, transparent)}.crumb{color:var(--color-text)}.crumb:last-child{color:var(--color-text)}.search-wrap{background:var(--color-bg-surface);border-color:var(--color-border)}.search-wrap:focus-within{border-color:var(--color-border)}#search{color:var(--color-text)}#search::placeholder{color:var(--color-text-muted)}.search-icon{color:var(--color-text)}.search-wrap kbd{border-color:var(--color-border);color:var(--color-text-muted)}.search-results{background:var(--color-bg-surface);border-color:var(--color-border)}.search-result:hover{background:var(--color-bg-elevated)}.search-result small{color:var(--color-text)}.view-switch{display:flex;gap:3px;border:1px solid var(--color-border);border-radius:6px;background:var(--color-bg-surface);padding:3px}.view-switch button{background:transparent;color:var(--color-text-muted);padding:6px 10px;font-size:11px;border-radius:4px}.view-switch button[aria-pressed=true]{background:var(--color-bg-elevated);color:var(--color-text)}.entity-filter{font-size:10px;color:var(--color-text-muted);display:flex;align-items:center;gap:10px}.entity-filter select{font:inherit;font-size:11px;color:var(--color-text);padding:6px 26px 6px 8px;background:var(--color-bg-surface);border:1px solid var(--color-border);border-radius:4px;max-width:155px}.entity-filter select:disabled{opacity:.45}.view-hint{margin-left:auto;font-size:10px;color:var(--color-text-muted)}\r\n.stage{background:radial-gradient(ellipse at 52% 45%,color-mix(in srgb, var(--color-bg-surface) 20%, transparent),transparent 66%),var(--color-bg-surface);min-height:500px}.constellation{background:radial-gradient(ellipse at 42% 44%,color-mix(in srgb, var(--color-bg-elevated) 10.2%, transparent),transparent 62%),var(--color-bg-surface)}.space-grid{opacity:.2;background-image:radial-gradient(color-mix(in srgb, var(--color-primary) 33.33%, transparent) .7px,transparent .7px);background-size:28px 28px}.orbit{display:none}.map-kicker{color:var(--color-text-muted);font-size:9px}\r\n.node{background:linear-gradient(145deg,var(--color-bg-elevated),var(--color-bg-surface));border-color:var(--color-border);color:var(--color-text);box-shadow:0 12px 30px color-mix(in srgb, var(--color-shadow) 20%, transparent)}.node:hover,.node:focus-visible{box-shadow:0 10px 35px color-mix(in srgb, var(--color-shadow) 33.33%, transparent),0 0 24px color-mix(in srgb, var(--color-accent) 12.55%, transparent)}.node.selected{background:linear-gradient(145deg,var(--color-bg-elevated),var(--color-bg-surface));box-shadow:0 0 40px color-mix(in srgb, var(--color-accent) 12.55%, transparent)}.root-node{background:var(--color-bg-elevated);border-color:var(--color-border)}.root-glyph{color:var(--color-text)}.node-meta{color:var(--color-text-muted)}.node-symbol{background:color-mix(in srgb, var(--color-primary) 9.41%, transparent)}.node-top .node-expand{color:var(--color-text)}.edge{stroke:var(--graph-edge);stroke-opacity:.4}.edge-flow{stroke:var(--graph-edge)}.edge-label{fill:var(--color-text);stroke:var(--graph-edge)}\r\n.inspector,dialog{background:var(--color-bg-surface);border-color:var(--color-border)}.inspector-header,.section-label{color:var(--color-text)}.entity-path{color:var(--color-text-muted)}.entity-summary{color:var(--color-text)}.icon-button{background:var(--color-bg-elevated);border-color:var(--color-border);color:var(--color-text)}.metric-grid{background:var(--color-bg-elevated);border-color:var(--color-border)}.metric{background:var(--color-bg-elevated)}.metric strong{color:var(--color-text)}.metric span{color:var(--color-text-muted)}.relationship{border-color:var(--color-border);color:var(--color-text)}.relationship small{color:var(--color-text-muted)}.relationship>span:first-child,.relationship>span:last-child{color:var(--color-text)}.inspector .quiet,.quiet{color:var(--color-text-muted)}.detail-tabs{border-color:var(--color-border)}.detail-tab{color:var(--color-text)}.signature{background:var(--color-bg-surface);border-color:var(--color-border);color:var(--color-text)}.history-entry{border-color:var(--color-border)}.history-entry:before{background:var(--color-primary)}.history-entry p{color:var(--color-text)}.changes-strip{border-color:var(--color-border);color:var(--color-text)}.detail-links a{border-color:var(--color-border)}.toast{background:var(--color-bg-elevated);border-color:var(--color-border);color:var(--color-text)}dialog::backdrop{background:color-mix(in srgb, var(--color-bg-surface) 72.16%, transparent)}.detail-panel{color:var(--color-text)}\r\n.legend-row{display:flex;align-items:center;gap:0;font-size:11px}.legend-row span{margin-left:auto;color:var(--color-text-muted);font-family:Consolas,monospace}.legend-row .dot{width:7px;height:7px}.module-color{background:var(--color-primary)}.class-color{background:var(--color-primary)}.file-color{background:var(--color-primary)}.function-color{background:var(--color-danger)}.data-color{background:var(--color-warning)}\r\n.node.orb{width:28px;min-height:28px;height:28px;padding:0;border:0;border-radius:5px;background:transparent;box-shadow:none;display:grid;place-items:center;overflow:visible;z-index:2;transition:opacity .2s}.node.orb:hover,.node.orb:focus-visible{box-shadow:none;background:transparent;z-index:8}.orb-shape{display:block;position:relative;width:9px;height:9px;border:1px solid color-mix(in srgb,var(--node-color),white 28%);background:color-mix(in srgb,var(--node-color),transparent 20%);border-radius:50%;box-shadow:0 0 9px color-mix(in srgb,var(--node-color),transparent 58%),inset 0 1px 3px color-mix(in srgb, var(--color-accent) 29.02%, transparent);transform:scale(calc(.85 + var(--depth) * .3));transition:transform .2s}\r\n.orb.module .orb-shape{width:22px;height:22px;border-radius:4px;background:color-mix(in srgb,var(--node-color),transparent 65%);box-shadow:0 0 17px color-mix(in srgb,var(--node-color),transparent 48%),inset 0 0 9px color-mix(in srgb,var(--node-color),transparent 65%);transform:rotate(-7deg)}.orb.class .orb-shape{width:13px;height:13px;border-radius:2px;transform:rotate(-8deg);box-shadow:0 0 13px color-mix(in srgb,var(--node-color),transparent 50%),inset 1px 2px 4px color-mix(in srgb, var(--color-accent) 33.33%, transparent)}.orb.module .orb-shape:after,.orb.class .orb-shape:after{content:'';position:absolute;inset:2px -3px -3px 2px;border:1px solid color-mix(in srgb,var(--node-color),transparent 28%);border-radius:2px;pointer-events:none}.orb.file .orb-shape{width:8px;height:8px;border-radius:2px;transform:rotate(35deg)}.orb.data .orb-shape{width:11px;height:11px;border-radius:3px;transform:rotate(45deg)}.orb:hover .orb-shape,.orb:focus-visible .orb-shape{transform:scale(1.7);filter:brightness(1.2)}\r\n.orb .node-name{position:absolute;left:34px;top:0;color:var(--color-text);margin:0;font-size:13px;line-height:22px;max-width:175px;white-space:nowrap;opacity:0;pointer-events:none;padding:1px 5px;background:color-mix(in srgb, var(--color-bg-surface) 73.33%, transparent);border-radius:3px;text-shadow:0 1px 6px var(--color-shadow);letter-spacing:.1px;transition:opacity .18s}.orb.module .node-name{opacity:1;top:6px;color:var(--color-text);letter-spacing:.4px;font-size:14px}.orb:hover .node-name,.orb:focus-visible .node-name,.zoomed .orb.class .node-name{opacity:1}.node.orb.dim{opacity:.12}.constellation .edge{stroke:var(--edge-color);stroke-width:.85;stroke-opacity:.23}.constellation .cross-link .edge{stroke-opacity:.35;stroke-width:1.15}.constellation .edge-flow{stroke:var(--edge-color);stroke-width:1.8;stroke-dasharray:2 195;stroke-opacity:.8;animation-duration:15s}.constellation .edge-hit{stroke-width:7}.constellation .edge-group.highlight .edge,.constellation .edge-group:hover .edge{stroke-width:1.8;stroke-opacity:.95}.constellation .edge-label{font-size:12px}.constellation .edge-group.dim{opacity:.09}.constellation .edge-group.highlight .edge-label{opacity:0}.constellation .edge-group:hover .edge-label{opacity:1}.node-tooltip{position:absolute;z-index:20;pointer-events:none;width:250px;background:color-mix(in srgb, var(--color-bg-elevated) 96.08%, transparent);border:1px solid var(--color-border);box-shadow:0 12px 40px color-mix(in srgb, var(--color-shadow) 40%, transparent);backdrop-filter:blur(12px);border-radius:9px;padding:14px;color:var(--color-text)}.tooltip-kind{font-size:9px;text-transform:uppercase;letter-spacing:1px;color:var(--color-text);margin-bottom:8px}.node-tooltip strong{font-size:14px;font-weight:500;overflow-wrap:anywhere}.tooltip-path{font-family:Consolas,monospace;font-size:10px;line-height:1.6;color:var(--color-text);margin-top:8px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.tooltip-action{font-size:10px;margin-top:12px;padding-top:9px;border-top:1px solid var(--color-border);color:var(--color-text)}\r\n@media(min-width:1550px){.main{padding-top:32px}.page-heading{margin-bottom:25px}.stage{min-height:610px}}\r\n@media(max-width:1150px){.view-hint{display:none}.view-switch button{font-size:10px}}\r\n@media(max-width:540px){h1{font-size:26px}.view-switch button{font-size:10px;padding:6px 7px}.entity-filter{font-size:9px;gap:5px}.entity-filter select{font-size:10px;max-width:126px;padding-right:15px}.map-kicker{font-size:8px}.stage{min-height:470px}}\r\n.orb-shape{scale:var(--glyph-scale,1)}.orb .node-name{font-size:var(--label-size,12px);line-height:var(--label-line,17px);max-width:270px;top:4px}.orb.module .node-name{font-size:var(--label-size,12px);top:4px;opacity:0}.orb.named .node-name{opacity:1}.orb.module:hover .node-name,.orb.module:focus-visible .node-name{opacity:1}.constellation .edge{stroke-width:1.1;stroke-opacity:.31}.constellation .cross-link .edge{stroke-width:1.3;stroke-opacity:.42}\n/* Canvas-first code explorer. Shared component styles live in atlas.css. */\r\n:root { --bg:var(--color-bg-base); --panel:var(--color-bg-surface); --surface:var(--color-bg-elevated); --line:var(--color-border); --text:var(--color-text); --muted:var(--color-text-muted); --mint:var(--color-primary);\r\n        \r\n    font-family:'Segoe UI',Arial,sans-serif; }\r\nhtml,body { height:100%; overflow:hidden; background:var(--bg); }\r\n#atlas { height:100dvh; min-height:0; display:grid; grid-template-columns:248px minmax(0,1fr); grid-template-rows:58px minmax(0,1fr); }\r\n.topbar { padding:0 18px; gap:18px; background:var(--color-bg-surface); border-bottom:1px solid var(--color-border); z-index:50; }\r\n.button { padding:9px 12px; font-size:11px; border-radius:7px; }\r\n.primary { background:var(--color-primary); color:var(--color-text-muted); font-weight:600; box-shadow:none; border:1px solid color-mix(in srgb, var(--color-border) 33.33%, transparent); }\r\n.primary:hover { background:var(--color-primary-dark); }\r\n.secondary { background:var(--color-bg-surface); color:var(--color-text); border:1px solid var(--color-border); }\r\n.secondary:hover { background:var(--color-bg-elevated); }\r\n.icon-button { background:transparent; border:1px solid transparent; color:var(--color-text-muted); border-radius:6px; width:28px; height:28px; }\r\n.icon-button:hover { background:var(--color-bg-elevated); color:var(--color-text); }\r\n.topbar>.search-wrap { margin-left:auto; width:270px; background:var(--color-bg-surface); border-color:var(--color-border); }\r\n#search { color:var(--color-text); font-size:11px; padding:8px 0; }\r\n#search::placeholder { color:var(--color-text-muted); }.search-icon { color:var(--color-text-muted); font-size:21px; }\r\n.search-wrap:focus-within { border-color:var(--color-border); }\r\n.search-wrap kbd { color:var(--color-text-muted); border-color:var(--color-border); }\r\n.search-results { background:var(--color-bg-surface); border-color:var(--color-border); max-height:min(460px,65vh); }\r\n.search-result { font-size:12px; }.search-result:hover { background:var(--color-bg-elevated); }.search-result small { color:var(--color-text-muted); }\r\n.main { grid-column:2; grid-row:2; min-width:0; min-height:0; padding:0; position:relative; }\r\n.map-shell { min-height:0; height:100%; border:0; border-radius:0; box-shadow:none; background:var(--bg); position:relative; overflow:hidden; }\r\n.map-body { min-height:0; position:absolute; inset:0; display:block; }\r\n.stage,.stage.constellation { position:absolute; inset:0; min-height:0; height:100%; background:var(--bg); }\r\n.crumb { color:var(--color-text-muted); font-size:11px; padding:7px 0; }.crumb:last-child { color:var(--color-text); }\r\n.view-switch { padding:3px; border:1px solid var(--color-border); background:color-mix(in srgb, var(--color-bg-surface) 92.94%, transparent); border-radius:7px; }\r\n.view-switch button { font-size:10px; padding:6px 9px; color:var(--color-text-muted); }\r\n.view-switch button[aria-pressed=true] { color:var(--color-text); background:color-mix(in srgb, var(--color-primary) 15.29%, transparent); }\r\n.entity-filter { gap:0; }.entity-filter>span { display:none; }\r\n.entity-filter select { max-width:142px; border:1px solid var(--color-border); background:color-mix(in srgb, var(--color-bg-surface) 92.94%, transparent); color:var(--color-text-muted); font-size:10px; padding:8px 18px 8px 10px; border-radius:7px; }\r\n.map-kicker { top:109px; left:29px; display:flex; flex-direction:column; align-items:flex-start; gap:6px; color:var(--color-text-muted); letter-spacing:1.2px; font-size:8px; }\r\n.view-hint { display:block; margin:0; font-size:10px; letter-spacing:0; color:var(--color-text-muted); }\r\n\r\n.legend-row { font-size:10px; color:var(--color-text); display:flex; align-items:center; gap:0; white-space:nowrap; }\r\n.legend-row span { margin-left:auto; color:var(--color-text-muted); font-size:9px; padding-left:5px; font-family:inherit; }\r\n.legend-row .dot { width:7px; height:7px; margin-right:7px; box-shadow:none; }\r\n.module-color { background:var(--node-module); }.class-color { background:var(--node-class); }.file-color { background:var(--node-file); }.function-color { background:var(--node-function); }.data-color { background:var(--node-data); }\r\n.node.orb,.node.orb.selected { width:28px; min-height:28px; height:28px; padding:0; border:0; border-radius:50%; background:none; box-shadow:none; pointer-events:none; z-index:2; }\r\n.node.orb::before { content:''; position:absolute; inset:5px; border-radius:50%; pointer-events:auto; }\r\n.node.orb .orb-shape { background:var(--node-color); border:0; border-radius:50%; width:8px; height:8px; box-shadow:none; transform:none; filter:none; opacity:var(--node-opacity,.94); scale:calc(var(--glyph-scale,1) * var(--depth-scale,1)); pointer-events:none; }\r\n.node.orb .orb-shape:before,.node.orb .orb-shape:after { content:none; }\r\n.node.orb.module .orb-shape { width:17px; height:17px; }.node.orb.class .orb-shape { width:11px; height:11px; }.node.orb.file .orb-shape { width:9px; height:9px; }.node.orb.function .orb-shape { width:6px; height:6px; }.node.orb.data .orb-shape { width:9px; height:9px; }\r\n.node.orb:hover,.node.orb:focus-visible,.node.orb.selected { z-index:1000 !important; }\r\n.node.orb:hover .orb-shape,.node.orb:focus-visible .orb-shape,.node.orb.selected .orb-shape { transform:scale(1.13); opacity:1; box-shadow:0 0 0 3px var(--bg),0 0 0 4px var(--node-color); }\r\n.node.orb.dim { opacity:.19; }.node.orb.selected { outline:none; }\r\n.node.orb .node-name,.node.orb.module .node-name,.node.orb.selected .node-name { opacity:0; left:calc(14px + 11px * var(--glyph-scale,1)); top:6px; padding:0; margin:0; background:none; text-shadow:0 1px 5px var(--bg),0 0 8px var(--bg); color:var(--color-text); font-size:var(--label-size,12px); line-height:var(--label-line,17px); font-weight:500; letter-spacing:0; text-transform:none; max-width:260px; }\r\n.node.orb.named .node-name { opacity:.8; }.node.orb.selected .node-name,.node.orb:hover .node-name,.node.orb:focus-visible .node-name { opacity:1; color:var(--color-text); }\r\n.constellation .edge { stroke:var(--graph-edge); stroke-width:.75; stroke-opacity:.23; }\r\n.constellation .cross-link .edge { stroke:var(--graph-cross-edge); stroke-width:1.25; stroke-opacity:.48; vector-effect:non-scaling-stroke; }\r\n.constellation .edge-flow { display:none; }\r\n.constellation .edge-group.highlight .edge,.constellation .edge-group:hover .edge { stroke:var(--edge-color); stroke-width:1.3; stroke-opacity:.8; }\r\n.constellation .edge-group.dim { opacity:.1; }.constellation .edge-hit { stroke-width:5; }\r\n.edge-label { fill:var(--color-text); stroke:var(--bg); }\r\n.node-tooltip { background:color-mix(in srgb, var(--color-bg-surface) 96.08%, transparent); border:1px solid var(--color-border); border-radius:9px; box-shadow:0 18px 45px color-mix(in srgb, var(--color-shadow) 33.33%, transparent); color:var(--color-text); backdrop-filter:blur(10px); }\r\n.tooltip-kind { color:var(--color-text); }.tooltip-path { color:var(--color-text-muted); }.tooltip-action { color:var(--color-text); border-color:var(--color-border); }\r\n.inspector { position:absolute; z-index:15; right:18px; top:65px; bottom:29px; max-height:none; width:310px; padding:21px; border:1px solid var(--color-border); border-radius:13px; background:color-mix(in srgb, var(--color-bg-surface) 96.08%, transparent); box-shadow:0 16px 55px color-mix(in srgb, var(--color-shadow) 33.33%, transparent); scrollbar-width:thin; scrollbar-color:var(--color-text-muted) transparent; }\r\n.inspector-header,.section-label { color:var(--color-text-muted); }.entity-path { color:var(--color-text-muted); }.entity-summary { color:var(--color-text); line-height:1.75; font-size:11px; }\r\n.inspector .button { width:100%; margin-right:0; }.inspector .metric-grid { grid-template-columns:1fr 1fr; }\r\n.metric-grid { background:var(--color-bg-elevated); border-color:var(--color-border); }.metric { background:var(--color-bg-elevated); }.metric strong { color:var(--color-text); font-size:20px; }.metric span { color:var(--color-text-muted); }\r\n.relationship { border-color:var(--color-border); color:var(--color-text); }.relationship small { color:var(--color-text-muted); }.relationship>span:first-child,.relationship>span:last-child { color:var(--color-text-muted); }\r\n.inspector .quiet,.quiet { color:var(--color-text-muted); }.node:not(.orb) { background:var(--color-bg-surface); border-color:var(--color-border); box-shadow:0 8px 22px color-mix(in srgb, var(--color-shadow) 20%, transparent); }.node:not(.orb).selected { background:var(--color-bg-elevated); border-color:var(--color-border); }.node-meta { color:var(--color-text-muted); }.node-symbol { background:color-mix(in srgb, var(--color-primary) 5.1%, transparent); }.root-glyph { color:var(--color-text-muted); }\r\ndialog { background:var(--color-bg-surface); border:1px solid var(--color-border); border-radius:15px; }.detail-tabs { border-color:var(--color-border); }.detail-tab { color:var(--color-text-muted); }.signature { background:var(--color-bg-base); color:var(--color-text); border-color:var(--color-border); }.history-entry { border-color:var(--color-border); }.history-entry:before { background:var(--color-primary); }.history-entry p { color:var(--color-text-muted); }.changes-strip { color:var(--color-text); border-color:var(--color-border); }.detail-links a { border-color:var(--color-border); }.detail-panel { color:var(--color-text); }\r\n.toast { background:var(--color-bg-elevated); color:var(--color-text); border-color:var(--color-border); }\r\n@media(max-width:1100px) { #atlas { grid-template-columns:218px minmax(0,1fr); }.topbar { gap:10px; padding:0 12px; }.topbar>.search-wrap { width:220px; }.map-kicker { display:none; }.inspector { top:96px; bottom:31px; }}\r\n@media(max-width:760px) { #atlas { grid-template-columns:0 minmax(0,1fr); grid-template-rows:100px minmax(0,1fr); }.topbar { position:relative; align-items:flex-start; padding-top:11px; gap:10px; }.button { padding:7px 9px; font-size:10px; }.topbar>.search-wrap { position:absolute; left:12px; right:12px; top:53px; width:auto; margin:0; }.search-results { width:100%; max-width:none; }.view-switch button { font-size:10px; }.entity-filter select { max-width:138px; font-size:10px; }.legend-row { font-size:9px; }.inspector { right:10px; left:10px; width:auto; top:95px; bottom:29px; max-height:none; padding:18px; }#dialog-content { padding:21px; }.detail-list { grid-template-columns:1fr; }.detail-panel .metric-grid { grid-template-columns:repeat(2,1fr); }}\r\n\r\n/* Search filters preserve spatial positions and remove hidden nodes from keyboard navigation. */\r\n.search-wrap.has-query { border-color:var(--color-border); }\r\n.search-wrap.has-query kbd { display:none; }\r\n#search::-webkit-search-cancel-button { display:none; }\r\n#clear-search { flex:0 0 25px; width:25px; height:25px; margin:0 5px; color:var(--color-text); font-size:19px; }\r\n.search-summary { padding:8px 10px 10px; color:var(--color-text-muted); font-size:10px; border-bottom:1px solid var(--color-border); margin-bottom:3px; }\r\n.empty-map { text-align:center; padding:30px; color:var(--color-text-muted); }\r\n.stage.rotate-mode { cursor:grab; }\r\n.stage.dragging,.stage.dragging * { cursor:grabbing; }\r\n.stage:focus-visible { outline:1px solid var(--color-border); outline-offset:-2px; }\r\n\r\n/* Brief reveals and a restrained signal on the relationships being inspected. */\r\n.node.orb .orb-pulse { position:absolute; inset:3px; border:1px solid var(--node-color); border-radius:50%; opacity:0; pointer-events:none; }\r\n.node.orb.selected .orb-pulse { animation:selection-ripple 850ms cubic-bezier(.16,1,.3,1) both; }\r\n@keyframes selection-ripple { from { opacity:.65; scale:.7; } to { opacity:0; scale:2.7; } }\r\n.constellation .edge-group.highlight .edge-flow { display:block; stroke:var(--edge-color); stroke-width:1.8; stroke-opacity:.85; stroke-linecap:round; stroke-dasharray:.028 .972; animation:connection-signal 2.8s linear infinite; pointer-events:none; }\r\n@keyframes connection-signal { from { stroke-dashoffset:1; } to { stroke-dashoffset:0; } }.view-switch button{ transition:background-color 180ms ease,color 180ms ease,border-color 180ms ease; }\r\n.page-hidden .orb-pulse,.page-hidden .edge-flow { animation-play-state:paused !important; }\r\n.page-hidden * { transition:none !important; }\r\n@media(prefers-reduced-motion:reduce) { .constellation .edge-flow,.orb-pulse { display:none !important; }}\r\n\r\n/* Consumer shell: optional branding and search above an uninterrupted graph. */\r\n#atlas { grid-template-columns:minmax(0,1fr); grid-template-rows:68px minmax(0,1fr); }\r\n.main { grid-column:1; grid-row:2; }\r\n.topbar { display:flex; align-items:center; justify-content:center; padding:0 24px; gap:24px; }\r\n.topbar>.search-wrap { position:relative; inset:auto; margin:0; width:min(560px,100%); background:var(--color-bg-surface); border-color:var(--color-border); }\r\n.topbar .search-results { width:100%; }\r\n.topbar:has(.consumer-brand) { justify-content:space-between; }\r\n.consumer-brand { display:flex; align-items:center; gap:11px; min-width:0; max-width:40%; color:var(--text); font-size:16px; font-weight:600; }\r\n.consumer-brand img { width:32px; height:32px; object-fit:contain; flex:0 0 auto; }\r\n.consumer-brand span { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }\r\n.back-button { flex:0 0 auto; pointer-events:auto; padding:7px 11px; min-height:32px; border:1px solid var(--color-border); border-radius:7px; background:var(--color-bg-surface); color:var(--color-text); font-size:11px; white-space:nowrap; }\r\n.back-button:hover:not(:disabled) { background:var(--color-bg-elevated); border-color:var(--color-border); }\r\n.back-button:disabled { opacity:.35; cursor:default; }\r\n#breadcrumbs { min-width:0; overflow:auto; white-space:nowrap; scrollbar-width:none; }\r\n#dialog-back { margin-bottom:16px; }\r\n@media(max-width:1100px) { .topbar { padding:0 18px; }.consumer-brand { font-size:14px; } }\r\n@media(max-width:760px) {\r\n    #atlas { grid-template-columns:minmax(0,1fr); grid-template-rows:58px minmax(0,1fr); }\r\n    #atlas:has(.consumer-brand) { grid-template-rows:104px minmax(0,1fr); }\r\n    .topbar { padding:0 12px; align-items:center; gap:8px; }\r\n    .topbar:has(.consumer-brand) { flex-direction:column; align-items:stretch; justify-content:center; }\r\n    .topbar>.search-wrap { position:relative; inset:auto; width:100%; margin:0; }\r\n    .consumer-brand { max-width:100%; justify-content:center; font-size:13px; gap:8px; }\r\n    .consumer-brand img { width:24px; height:24px; }\r\n    .back-button { padding:6px 9px; font-size:10px; }\r\n}\r\n\r\n/* A single wrapping control strip occupies its own row, clear of the inspector. */\r\n.map-shell { display:flex; flex-direction:column; }\r\n.map-body { position:relative; inset:auto; flex:1; min-height:0; }\r\n.graph-toolbar { display:flex; flex:0 0 auto; flex-wrap:wrap; align-items:center; gap:8px 14px; padding:10px 16px; background:var(--color-bg-surface); border-bottom:1px solid var(--color-border); z-index:20; }\r\n.graph-location { display:flex; align-items:center; gap:9px; flex:1 1 160px; min-width:0; }\r\n.graph-location nav { display:flex; align-items:center; gap:8px; min-width:0; font-size:11px; color:var(--muted); }\r\n.view-controls,.navigation-controls,.zoom-controls { display:flex; align-items:center; gap:3px; flex:0 0 auto; }\r\n.view-controls { gap:9px; }\r\n.navigation-controls,.zoom-controls { border-left:1px solid var(--color-border); padding-left:10px; }\r\n.graph-toolbar button { border-radius:6px; min-height:32px; padding:6px 8px; background:transparent; color:var(--color-text); font-size:11px; white-space:nowrap; transition:background-color 180ms ease,color 180ms ease; }\r\n.graph-toolbar button:hover:not(:disabled) { background:var(--color-bg-elevated); color:var(--color-text); }\r\n.graph-toolbar button[aria-pressed=true] { color:var(--color-text); background:color-mix(in srgb, var(--color-primary) 14.9%, transparent); }\r\n.graph-toolbar button:disabled { opacity:.4; }\r\n.graph-toolbar .back-button { border:1px solid var(--color-border); }\r\n.graph-toolbar .crumb { padding:6px 0; min-height:32px; }\r\n.graph-toolbar .view-switch { display:flex; padding:2px; background:var(--color-bg-surface); border-color:var(--color-border); }\r\n.graph-toolbar .view-switch button { min-height:28px; }\r\n.graph-toolbar .entity-filter select { height:34px; font-size:11px; }\r\n.zoom-controls output { min-width:34px; text-align:center; color:var(--color-text-muted); font-size:10px; }\r\n#zoom-in,#zoom-out { width:29px; padding:0; font-size:18px; }\r\n.map-kicker { top:18px; left:19px; }\r\n.inspector { top:12px; }\r\n@media(max-width:1250px) { .graph-location { flex-basis:100%; }.graph-toolbar { gap:7px 14px; } }\r\n@media(max-width:760px) {\r\n    .graph-toolbar { padding:8px 12px; gap:6px; }\r\n    .graph-location { flex-basis:100%; }\r\n    .view-controls { width:100%; justify-content:space-between; }\r\n    .navigation-controls,.zoom-controls { padding-left:0; border-left:0; }\r\n    .graph-toolbar button { font-size:10px; padding:6px 8px; }\r\n    .navigation-controls { width:100%; }\r\n    .zoom-controls { width:100%; gap:3px; justify-content:space-between; }\r\n    .graph-toolbar .entity-filter select { max-width:138px; font-size:10px; }\r\n    .inspector { top:10px; }\r\n}\r\n\r\n/* Summary owns a bottom row so neither nodes nor the inspector can be covered. */\r\n.graph-summary { display:grid; grid-template-columns:minmax(0,1fr); flex:0 0 auto; align-items:center; gap:6px 24px; padding:10px 18px; border-top:1px solid var(--color-border); background:var(--color-bg-surface); }\r\n.graph-legend { display:flex; align-items:center; gap:18px; min-width:0; }\r\n.legend-title { flex:0 0 auto; margin:0; font-size:9px; font-weight:400; letter-spacing:.7px; text-transform:uppercase; color:var(--color-text-muted); }\r\n.legend-items { display:flex; flex:1; flex-wrap:wrap; align-items:center; gap:6px 18px; min-width:0; }\r\n.inspector { bottom:12px; }\r\n@media(max-width:900px) {\r\n    .graph-summary { grid-template-columns:minmax(0,1fr); padding:8px 12px; }\r\n    .graph-legend { gap:14px; }\r\n    .legend-items { gap:6px 14px; }\r\n    .inspector { bottom:10px; }\r\n}\r\n\r\n/* Changed entities keep their type colors while unchanged entities dim. */\r\n.node.change-match:not(.orb) { border-color:var(--graph-changed); box-shadow:0 0 0 2px color-mix(in srgb, var(--graph-changed) 33.33%, transparent),0 0 22px color-mix(in srgb, var(--color-accent) 14.9%, transparent); }\r\n.node.orb.change-match .orb-shape { opacity:1; box-shadow:0 0 0 2px var(--bg),0 0 0 3.5px var(--graph-changed),0 0 15px color-mix(in srgb, var(--graph-changed) 60%, transparent); }\r\n.node.orb.change-match:hover .orb-shape,.node.orb.change-match:focus-visible .orb-shape,.node.orb.change-match.selected .orb-shape { box-shadow:0 0 0 3px var(--bg),0 0 0 4px var(--node-color),0 0 0 6px var(--bg),0 0 0 7px var(--graph-changed); }\r\n.edge-group.change-muted { opacity:.25; }\r\n.change-summary { grid-column:1/-1; margin:0; min-width:0; font-size:10px; line-height:1.6; color:var(--color-text); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }\r\n.change-summary::before { content:''; display:inline-block; width:7px; height:7px; border:1.5px solid var(--graph-changed); border-radius:50%; margin-right:6px; }\r\n#highlight-changes { border:1px solid var(--color-border); }\r\n#highlight-changes[aria-pressed=true] { border-color:var(--graph-changed); background:color-mix(in srgb, var(--color-accent) 9.41%, transparent); color:var(--color-text); }\r\n\r\n/* One small loading scene, independent of graph layout and camera motion. */\r\n.atlas-loader { position:fixed; inset:0; z-index:100; display:flex; flex-direction:column; align-items:center; justify-content:center; background:var(--bg); text-align:center; padding:24px; }\r\n.loading-visual { width:min(320px,78vw); margin-bottom:12px; animation:neuron-drift 4s ease-in-out infinite alternate; }\r\n.loading-neurons { display:block; width:100%; height:auto; overflow:visible; }\r\n.loading-threads { opacity:.35; }\r\n.loading-points { animation:neuron-breathe 2.2s ease-in-out infinite alternate; }\r\n.loading-signals { opacity:.4; animation:neuron-breathe 1.8s ease-in-out -.9s infinite alternate; }\r\n.loading-caption { margin:0; color:var(--color-text); font-size:14px; font-weight:500; letter-spacing:.2px; }\r\n.loading-description { margin:9px 0 0; max-width:340px; color:var(--color-text-muted); font-size:11px; line-height:1.6; overflow-wrap:anywhere; }\r\nbody[data-startup=loading] #atlas,body[data-startup=error] #atlas { opacity:0; }\r\nbody[data-startup=error] .loading-visual,body[data-startup=error] .loading-points,body[data-startup=error] .loading-signals { animation:none; }\r\n@keyframes neuron-drift { from { transform:translateY(3px) rotate(-3deg); } to { transform:translateY(-3px) rotate(3deg); } }\r\n@keyframes neuron-breathe { from { opacity:.4; } to { opacity:.95; } }\r\n.page-hidden .loading-visual,.page-hidden .loading-points,.page-hidden .loading-signals { animation-play-state:paused; }\r\n@media(prefers-reduced-motion:reduce) { .loading-visual,.loading-points,.loading-signals { animation:none; } }\r\n\r\n/* Semantic roles shared with the host theme; all geometry stays independent of color. */\r\n.primary { background:var(--color-primary); color:var(--color-on-primary); border-color:var(--color-primary); }\r\n.primary:hover { background:var(--color-primary-dark); }\r\n.node:not(.orb).selected { background:color-mix(in srgb,var(--color-primary) 12%,var(--color-bg-elevated)); border-color:var(--color-primary); }\r\n.node.change-match:not(.orb) { border-color:var(--graph-changed); box-shadow:0 0 0 2px color-mix(in srgb,var(--graph-changed) 35%,transparent),0 0 22px color-mix(in srgb,var(--graph-changed) 15%,transparent); }\r\n#highlight-changes[aria-pressed=true] { color:var(--graph-changed); border-color:var(--graph-changed); background:color-mix(in srgb,var(--graph-changed) 10%,transparent); }\r\n.change-summary { color:var(--color-text-muted); }\r\n.change-summary::before { border-color:var(--graph-changed); }\r\n.constellation .edge { stroke:var(--graph-edge); }\r\n.constellation .cross-link .edge { stroke:var(--graph-cross-edge); }\r\n.constellation .edge-group.highlight .edge,.constellation .edge-group:hover .edge { stroke:var(--edge-color); }\r\n.edge { stroke:var(--graph-edge); }.edge-flow { stroke:var(--color-accent); }\r\n\r\n.secondary:hover,.icon-button:hover,.search-result:hover,.back-button:hover:not(:disabled),.graph-toolbar button:hover:not(:disabled) { background:var(--color-bg-surface-hover); }\r\n.toast.error { color:var(--color-danger); border-color:var(--color-danger); }\r\n\r\n.view-summary { margin:0; color:var(--color-text-muted); font-size:10px; line-height:1.5; }\r\n\r\n.legend-row span { min-width:4ch; text-align:right; }\r\n\u003c/style>\r\n\u003cstyle id=\"atlas-theme\">\u003c!-- ATLAS_THEME -->\u003c/style>\r\n\u003cstyle id=\"workbench-atlas-controls\">\n/* Prototype-only layout: one filter row and a camera rail inside the canvas. */\n#atlas { grid-template-rows:56px minmax(0,1fr); }\n.topbar.graph-toolbar {\n  min-width:0; padding:0 12px; gap:10px; flex-wrap:nowrap;\n  justify-content:flex-start; align-items:center; z-index:50;\n}\n.topbar .graph-location { flex:0 0 auto; min-width:0; gap:8px; max-width:240px; }\n.topbar #breadcrumbs { max-width:160px; }\n.topbar>.search-wrap {\n  position:relative; inset:auto; margin:0; width:auto; min-width:160px;\n  flex:1 1 260px; height:34px; background:var(--color-bg-base);\n}\n.topbar #search { font-size:11px; }\n.topbar .view-controls { width:auto; flex:0 0 auto; gap:10px; }\n.topbar #highlight-changes { flex:0 0 auto; border:1px solid var(--color-border); }\n.topbar button { font-size:11px; }\n.topbar .entity-filter select { max-width:138px; font-size:11px; }\n.topbar .search-results { width:max(100%,340px); max-width:calc(100vw - 24px); left:0; right:auto; top:41px; }\n.map-body { background:var(--bg); }\n.stage,.stage.constellation { right:104px; }\n.canvas-controls {\n  position:absolute; right:12px; top:14px; z-index:20;\n  display:flex; flex-direction:column; gap:6px; width:82px; padding:6px;\n  max-height:calc(100% - 28px); overflow-y:auto; scrollbar-width:thin;\n  border:1px solid var(--color-border); border-radius:10px;\n  background:var(--color-bg-surface);\n  box-shadow:0 6px 24px color-mix(in srgb,var(--color-shadow) 15%,transparent);\n}\n.canvas-controls .navigation-controls,.canvas-controls .zoom-controls {\n  display:flex; flex-direction:column; align-items:stretch; gap:3px;\n  width:100%; border-left:0; padding:0; flex:0 0 auto;\n}\n.canvas-controls .zoom-controls { border-top:1px solid var(--color-border); padding-top:6px; }\n.canvas-controls button {\n  min-height:32px; width:100%; padding:6px 2px; border:0; border-radius:6px;\n  color:var(--color-text); background:transparent; font-size:10px; white-space:nowrap;\n  transition:background-color 180ms ease,color 180ms ease;\n}\n.canvas-controls button:hover:not(:disabled) { background:var(--color-bg-surface-hover); }\n.canvas-controls button[aria-pressed=true] { background:color-mix(in srgb,var(--color-primary) 14.9%,transparent); }\n.canvas-controls button:disabled { opacity:.4; }\n.canvas-controls button:focus-visible { outline:2px solid var(--color-primary); outline-offset:-2px; }\n.canvas-controls #zoom-in,.canvas-controls #zoom-out { width:100%; min-height:30px; padding:0; font-size:20px; }\n.canvas-controls #zoom-value { min-width:0; padding:2px 0; text-align:center; color:var(--color-text-muted); font-size:10px; }\n.canvas-controls #fit-button { border-top:1px solid var(--color-border); border-radius:0 0 6px 6px; margin-top:3px; }\n.inspector { right:104px; }\n@media (max-width:980px) {\n  .topbar #breadcrumbs { max-width:95px; }\n  .topbar.graph-toolbar,.topbar .view-controls { gap:8px; }\n}\n@media (max-width:820px) {\n  .topbar.graph-toolbar { overflow-x:auto; overflow-y:hidden; scrollbar-width:thin; scrollbar-color:var(--color-border) transparent; }\n  .topbar .graph-location { flex-basis:auto; }\n  .topbar>.search-wrap { flex:1 0 180px; }\n  .topbar .search-results { position:fixed; top:56px; left:12px; right:12px; width:auto; max-width:none; }\n}\n@media (max-width:760px) {\n  .inspector { left:10px; right:104px; width:auto; }\n}\n\u003c/style>\n\u003c/head>\r\n\u003cbody data-startup=\"loading\">\r\n\u003cdiv id=\"atlas-loader\" class=\"atlas-loader\" role=\"status\" aria-live=\"polite\">\r\n  \u003cdiv class=\"loading-visual\" aria-hidden=\"true\">\r\n    \u003csvg viewBox=\"0 0 320 230\" fill=\"none\" class=\"loading-neurons\">\r\n      \u003cdefs>\u003clinearGradient id=\"loading-thread\" x1=\"55\" y1=\"35\" x2=\"265\" y2=\"200\" gradientUnits=\"userSpaceOnUse\">\u003cstop stop-color=\"var(--node-module)\"/>\u003cstop offset=\".5\" stop-color=\"var(--node-data)\"/>\u003cstop offset=\"1\" stop-color=\"var(--node-file)\"/>\u003c/linearGradient>\u003c/defs>\r\n      \u003cg class=\"loading-threads\" stroke=\"url(#loading-thread)\" stroke-width=\"1\">\r\n        \u003cpath d=\"M40 116 69 70 112 52 147 85 181 44 226 66 275 105 247 151 203 181 167 153 124 194 81 158 40 116Z\"/>\r\n        \u003cpath d=\"M69 70 99 112 81 158 127 137 147 85 173 113 226 66 218 119 247 151 192 142 203 181M40 116 99 112 127 137 167 153 192 142 218 119 275 105M112 52 99 112 147 85 181 44M127 137 173 113 192 142M173 113 218 119M124 194 127 137M167 153 173 113\"/>\r\n        \u003cpath d=\"M24 67 69 70M82 29 112 52M181 44 219 25M275 105 299 145M247 151 259 191M81 158 50 193M203 181 191 208\" opacity=\".45\"/>\r\n      \u003c/g>\r\n      \u003cg class=\"loading-points\" fill=\"var(--node-data)\">\r\n        \u003ccircle cx=\"40\" cy=\"116\" r=\"3\"/>\u003ccircle cx=\"69\" cy=\"70\" r=\"4\"/>\u003ccircle cx=\"112\" cy=\"52\" r=\"4\" fill=\"var(--node-class)\"/>\r\n        \u003ccircle cx=\"147\" cy=\"85\" r=\"5\" fill=\"var(--node-class)\"/>\u003ccircle cx=\"181\" cy=\"44\" r=\"3\"/>\u003ccircle cx=\"226\" cy=\"66\" r=\"4\" fill=\"var(--node-file)\"/>\r\n        \u003ccircle cx=\"275\" cy=\"105\" r=\"3\"/>\u003ccircle cx=\"247\" cy=\"151\" r=\"4\" fill=\"var(--node-class)\"/>\u003ccircle cx=\"203\" cy=\"181\" r=\"3\"/>\r\n        \u003ccircle cx=\"167\" cy=\"153\" r=\"4\" fill=\"var(--node-file)\"/>\u003ccircle cx=\"124\" cy=\"194\" r=\"3\"/>\u003ccircle cx=\"81\" cy=\"158\" r=\"4\" fill=\"var(--node-module)\"/>\r\n        \u003ccircle cx=\"99\" cy=\"112\" r=\"4\" fill=\"var(--node-file)\"/>\u003ccircle cx=\"127\" cy=\"137\" r=\"3\"/>\u003ccircle cx=\"173\" cy=\"113\" r=\"5\" fill=\"var(--node-class)\"/>\r\n        \u003ccircle cx=\"192\" cy=\"142\" r=\"3\"/>\u003ccircle cx=\"218\" cy=\"119\" r=\"4\" fill=\"var(--node-module)\"/>\r\n        \u003cg opacity=\".5\">\u003ccircle cx=\"24\" cy=\"67\" r=\"2\"/>\u003ccircle cx=\"82\" cy=\"29\" r=\"2\"/>\u003ccircle cx=\"219\" cy=\"25\" r=\"2\"/>\u003ccircle cx=\"299\" cy=\"145\" r=\"2\"/>\u003ccircle cx=\"259\" cy=\"191\" r=\"2\"/>\u003ccircle cx=\"50\" cy=\"193\" r=\"2\"/>\u003ccircle cx=\"191\" cy=\"208\" r=\"2\"/>\u003c/g>\r\n      \u003c/g>\r\n      \u003cg class=\"loading-signals\" stroke=\"var(--color-accent)\">\u003ccircle cx=\"147\" cy=\"85\" r=\"10\"/>\u003ccircle cx=\"218\" cy=\"119\" r=\"9\"/>\u003ccircle cx=\"81\" cy=\"158\" r=\"8\"/>\u003c/g>\r\n    \u003c/svg>\r\n  \u003c/div>\r\n  \u003cp class=\"loading-caption\">Preparing your code graph\u003c/p>\r\n  \u003cp class=\"loading-description\">Bringing the connections into view\u003c/p>\r\n\u003c/div>\r\n\u003cdiv id=\"atlas\" inert aria-busy=\"true\">\r\n  \u003cheader id=\"graph-toolbar\" class=\"topbar graph-toolbar\" aria-label=\"Code explorer controls\">\n\u003cdiv class=\"graph-location\">\u003cbutton id=\"back-button\" class=\"back-button\" aria-label=\"Back to previous screen\" title=\"Back to previous screen\" disabled>← Back\u003c/button>\u003cnav id=\"breadcrumbs\" aria-label=\"Map breadcrumb\">\u003c/nav>\u003c/div>\n\u003cdiv class=\"search-wrap\">\u003clabel for=\"search\" class=\"search-icon\">⌕\u003c/label>\u003cinput id=\"search\" type=\"search\" placeholder=\"Search code…\" autocomplete=\"off\" aria-label=\"Search code\">\u003ckbd>/\u003c/kbd>\u003cbutton id=\"clear-search\" class=\"icon-button\" type=\"button\" aria-label=\"Clear search\" title=\"Clear search (Esc)\" hidden>×\u003c/button>\u003cdiv id=\"search-results\" class=\"search-results\" hidden>\u003c/div>\u003c/div>\n\u003cdiv class=\"view-controls\">\u003cdiv class=\"view-switch\" role=\"group\" aria-label=\"Map style\">\u003cbutton id=\"constellation-button\" aria-pressed=\"true\">Code graph\u003c/button>\u003cbutton id=\"cards-button\" aria-pressed=\"false\">Cards\u003c/button>\u003c/div>\u003clabel class=\"entity-filter\">\u003cspan>Show\u003c/span>\u003cselect id=\"entity-filter\" aria-label=\"Entity visibility\">\u003coption value=\"all\">All entities\u003c/option>\u003coption value=\"classes\">Classes &amp; types\u003c/option>\u003coption value=\"files\">Files\u003c/option>\u003coption value=\"data\">Data &amp; SQL\u003c/option>\u003c/select>\u003c/label>\u003c/div>\r\n        \u003cbutton id=\"highlight-changes\" aria-pressed=\"false\" aria-describedby=\"change-summary\" hidden>Highlight changes\u003c/button>\n\u003c/header>\r\n  \u003cmain class=\"main\">\r\n    \u003csection class=\"map-shell\" aria-label=\"Interactive code map\">\r\n      \u003cdiv class=\"map-body\">\n\u003cdiv class=\"canvas-controls\" role=\"group\" aria-label=\"Canvas controls\">\u003cdiv id=\"orbit-controls\" class=\"navigation-controls\" role=\"group\" aria-label=\"Graph navigation\">\r\n          \u003cbutton id=\"pan-mode\" aria-pressed=\"true\" title=\"Drag canvas or use arrow keys to pan\">✥ Pan\u003c/button>\r\n          \u003cbutton id=\"rotate-mode\" aria-pressed=\"false\" title=\"Drag canvas or use arrow keys to rotate; returns to Pan after 3 seconds idle\">↺ Rotate\u003c/button>\r\n        \u003c/div>\r\n        \u003cdiv class=\"zoom-controls\" role=\"group\" aria-label=\"Graph view\">\r\n          \u003cbutton id=\"zoom-in\" aria-label=\"Zoom in\" title=\"Zoom in\">+\u003c/button>\u003coutput id=\"zoom-value\" aria-label=\"Zoom level\">100%\u003c/output>\u003cbutton id=\"zoom-out\" aria-label=\"Zoom out\" title=\"Zoom out\">−\u003c/button>\r\n          \u003cbutton id=\"fit-button\" aria-label=\"Fit graph\" title=\"Fit graph\">⛶ Fit\u003c/button>\r\n          \u003cbutton id=\"reset-view\" title=\"Return to the front and fit the graph\">Reset view\u003c/button>\r\n          \u003cbutton id=\"reset-layout\" aria-label=\"Reset layout\" title=\"Reset moved nodes\">Reset nodes\u003c/button>\r\n        \u003c/div>\u003c/div>\r\n        \u003cdiv id=\"stage\" class=\"stage\" tabindex=\"0\" aria-label=\"Code graph. Drag to pan, scroll to zoom. Select Rotate to turn the graph.\">\r\n          \u003cdiv class=\"map-kicker\">\u003cspan id=\"view-label\">REPOSITORY OVERVIEW\u003c/span>\u003cspan class=\"view-hint\" id=\"view-hint\">\u003c/span>\u003c/div>\r\n          \u003cdiv id=\"world\" class=\"world\">\u003csvg id=\"connections\" aria-label=\"Directed code relationships\">\u003cdefs>\u003cmarker id=\"arrow\" viewBox=\"0 0 10 10\" refX=\"9\" refY=\"5\" markerWidth=\"5\" markerHeight=\"5\" orient=\"auto-start-reverse\">\u003cpath d=\"M 0 0 L 10 5 L 0 10 z\" fill=\"var(--graph-edge)\"/>\u003c/marker>\u003c/defs>\u003cg id=\"edge-layer\">\u003c/g>\u003c/svg>\u003cdiv id=\"nodes\">\u003c/div>\u003c/div>\r\n          \u003cdiv id=\"node-tooltip\" class=\"node-tooltip\" role=\"tooltip\" hidden>\u003c/div>\r\n          \u003cdiv id=\"empty-map\" class=\"empty-map\" hidden>No entities in this view.\u003c/div>\r\n        \u003c/div>\r\n        \u003caside id=\"inspector\" class=\"inspector\" aria-label=\"Selected code entity\" hidden>\u003c/aside>\r\n      \u003c/div>\r\n      \u003cdiv class=\"graph-summary\" role=\"group\" aria-label=\"Graph summary\">\r\n        \u003cdiv class=\"graph-legend\" role=\"group\" aria-label=\"Node types and counts\">\u003cdiv class=\"legend-title\">Node types\u003c/div>\u003cdiv class=\"legend-items\">\u003cdiv class=\"legend-row\">\u003ci class=\"dot module-color\">\u003c/i>Module \u003cspan id=\"count-module\">\u003c/span>\u003c/div>\u003cdiv class=\"legend-row\">\u003ci class=\"dot class-color\">\u003c/i>Class / type \u003cspan id=\"count-class\">\u003c/span>\u003c/div>\u003cdiv class=\"legend-row\">\u003ci class=\"dot file-color\">\u003c/i>File \u003cspan id=\"count-file\">\u003c/span>\u003c/div>\u003cdiv class=\"legend-row\">\u003ci class=\"dot function-color\">\u003c/i>Function \u003cspan id=\"count-function\">\u003c/span>\u003c/div>\u003cdiv class=\"legend-row\">\u003ci class=\"dot data-color\">\u003c/i>Data / SQL \u003cspan id=\"count-data\">\u003c/span>\u003c/div>\u003c/div>\u003c/div>\r\n        \u003cp id=\"view-summary\" class=\"view-summary\" role=\"status\" hidden>\u003c/p>\r\n        \u003cp id=\"change-summary\" class=\"change-summary\" aria-live=\"polite\" hidden>\u003c/p>\r\n      \u003c/div>\r\n    \u003c/section>\r\n  \u003c/main>\r\n  \u003cdialog id=\"details-dialog\" aria-labelledby=\"detail-title\">\u003cdiv id=\"dialog-content\">\u003c/div>\u003c/dialog>\r\n  \u003cdiv id=\"toast\" class=\"toast\" role=\"status\" hidden>\u003c/div>\r\n\u003c/div>\r\n\u003cnoscript>\u003cstyle>#atlas-loader,#atlas{display:none}\u003c/style>\u003cp>Enable JavaScript to explore this code graph.\u003c/p>\u003c/noscript>\r\n\u003cscript id=\"atlas-data\" type=\"application/json\">\u003c!-- ATLAS_DATA -->\u003c/script>\r\n\u003cscript id=\"atlas-options\" type=\"application/json\">\u003c!-- ATLAS_OPTIONS -->\u003c/script>\r\n\u003cscript>(function (root) {\n    'use strict';\n    const MAX_NODES = 3000;\n    const MAX_EDGES = 15000;\n    const MAX_BYTES = 8 * 1024 * 1024;\n    function assert(ok, message) { if (!ok) throw new Error(message); }\n    function object(value, label) {\n        assert(value && typeof value === 'object' && !Array.isArray(value), `${label} must be an object.`);\n    }\n    function string(value, label, max = 20000) {\n        assert(typeof value === 'string' && value.trim().length > 0 && value.length \u003c= max,\n            `${label} must be a non-empty string (up to ${max} characters).`);\n    }\n    function optionalStrings(value, fields, label) {\n        for (const field of fields) if (value[field] !== undefined) string(value[field], `${label}.${field}`);\n    }\n    function safeUrl(value) {\n        try { const url = new URL(value); return ['https:', 'http:'].includes(url.protocol) ? url.href : null; }\n        catch { return null; }\n    }\n    function validate(graph) {\n        object(graph, 'Graph');\n        assert(graph.schemaVersion === '1.0', 'schemaVersion must be \"1.0\".');\n        object(graph.repository, 'repository');\n        string(graph.repository.name, 'repository.name', 200);\n        optionalStrings(graph.repository, ['description', 'branch', 'revision', 'generatedAt'], 'repository');\n        if (graph.repository.sample !== undefined) assert(typeof graph.repository.sample === 'boolean', 'repository.sample must be boolean.');\n        assert(Array.isArray(graph.nodes) && graph.nodes.length \u003c= MAX_NODES, `nodes must be an array of at most ${MAX_NODES} items.`);\n        assert(Array.isArray(graph.edges) && graph.edges.length \u003c= MAX_EDGES, `edges must be an array of at most ${MAX_EDGES} items.`);\n        const ids = new Map();\n        for (const node of graph.nodes) {\n            object(node, 'Node');\n            for (const field of ['id', 'name', 'kind']) string(node[field], `Node.${field}`, 500);\n            assert(!ids.has(node.id), `Duplicate node id: ${node.id}`);\n            ids.set(node.id, node);\n            optionalStrings(node, ['parentId', 'language', 'path', 'summary', 'signature'], node.id);\n            for (const field of ['metrics', 'changes']) {\n                if (node[field] === undefined) continue;\n                object(node[field], `${node.id}.${field}`);\n                for (const [key, value] of Object.entries(node[field])) {\n                    assert(typeof value === 'number' && Number.isFinite(value) && value >= 0,\n                        `${node.id}.${field}.${key} must be a finite non-negative number.`);\n                    if (key === 'coverage') assert(value \u003c= 100, `${node.id} coverage must be 0–100.`);\n                }\n            }\n            if (node.history !== undefined) {\n                assert(Array.isArray(node.history) && node.history.length \u003c= 100, `${node.id}.history must contain at most 100 entries.`);\n                for (const h of node.history) {\n                    object(h, `${node.id}.history entry`);\n                    for (const key of ['commit', 'message', 'author', 'date']) string(h[key], `history.${key}`);\n                    assert(!Number.isNaN(Date.parse(h.date)), `Invalid history date for ${node.id}.`);\n                    if (h.url !== undefined) assert(safeUrl(h.url), 'History URLs must be absolute HTTP(S) URLs.');\n                }\n            }\n            if (node.links !== undefined) {\n                assert(Array.isArray(node.links) && node.links.length \u003c= 20, `${node.id}.links must contain at most 20 entries.`);\n                for (const link of node.links) {\n                    object(link, 'Link'); string(link.label, 'Link.label', 200);\n                    assert(safeUrl(link.url), 'Link URLs must be absolute HTTP(S) URLs.');\n                }\n            }\n        }\n        for (const node of graph.nodes) {\n            const seen = new Set([node.id]);\n            let parent = node.parentId;\n            while (parent !== undefined) {\n                assert(ids.has(parent), `Missing parent ${parent} for ${node.id}.`);\n                assert(!seen.has(parent), `Parent cycle involving ${node.id}.`);\n                seen.add(parent); parent = ids.get(parent).parentId;\n                assert(seen.size \u003c= 64, 'Hierarchy depth must not exceed 64.');\n            }\n        }\n        const edgeIds = new Set();\n        for (const edge of graph.edges) {\n            object(edge, 'Edge');\n            for (const key of ['id', 'source', 'target', 'kind']) string(edge[key], `Edge.${key}`, 500);\n            assert(!edgeIds.has(edge.id), `Duplicate edge id: ${edge.id}`);\n            edgeIds.add(edge.id);\n            assert(ids.has(edge.source) && ids.has(edge.target), `Edge ${edge.id} references a missing node.`);\n        }\n        return graph;\n    }\n    function index(graph) {\n        validate(graph);\n        const nodes = new Map(graph.nodes.map(n => [n.id, n]));\n        const children = new Map([[null, []]]);\n        const adjacency = new Map(graph.nodes.map(n => [n.id, []]));\n        for (const node of graph.nodes) {\n            const parent = node.parentId ?? null;\n            if (!children.has(parent)) children.set(parent, []);\n            children.get(parent).push(node);\n        }\n        for (const edge of graph.edges) {\n            adjacency.get(edge.source).push(edge);\n            if (edge.source !== edge.target) adjacency.get(edge.target).push(edge);\n        }\n        return { nodes, children, adjacency };\n    }\n    // Project symbol-level edges onto visible ancestors at the current hierarchy level.\n    function aggregateEdges(graph, visibleIds, nodes) {\n        const visible = new Set(visibleIds);\n        const owners = new Map();\n        function owner(id) {\n            if (owners.has(id)) return owners.get(id);\n            let current = id;\n            while (current && !visible.has(current)) current = nodes.get(current)?.parentId;\n            owners.set(id, current); return current;\n        }\n        const result = new Map();\n        for (const edge of graph.edges) {\n            const source = owner(edge.source), target = owner(edge.target);\n            if (!source || !target || (source === target && edge.source !== edge.target)) continue;\n            const key = JSON.stringify([source, target, edge.kind]);\n            if (!result.has(key)) result.set(key, { source, target, kind: edge.kind, count: 0 });\n            result.get(key).count++;\n        }\n        return [...result.values()];\n    }\n    // JSON sits in a raw-text script element. Escape '\u003c' so data cannot terminate it.\n    function serialize(graph) {\n        return JSON.stringify(graph).replace(/\u003c/g, '\\\\u003c').replace(/\\u2028/g, '\\\\u2028').replace(/\\u2029/g, '\\\\u2029');\n    }\n    root.CodeAtlasContract = Object.freeze({ validate, index, aggregateEdges, serialize, safeUrl, MAX_NODES, MAX_EDGES, MAX_BYTES });\n})(globalThis);\n\u003c/script>\r\n\u003cscript>(function (root) {\r\n    'use strict';\r\n    const MAX_FILES = 10000;\r\n    // Repository-relative, case-sensitive paths. Never match just a basename or a prefix.\r\n    function normalizePath(value, location = false) {\r\n        if (typeof value !== 'string' || !value.trim() || value.length > 4096 || /[\\u0000-\\u001f\\u007f]/.test(value)) return null;\r\n        let path = value.replace(/\\\\/g, '/');\r\n        if (location) path = path.replace(/:\\d+(?::\\d+)?$/, '');\r\n        if (path.startsWith('/') || /^[a-z][a-z\\d+.-]*:/i.test(path) || path.endsWith('/')) return null;\r\n        const parts = path.split('/').filter(part => part && part !== '.');\r\n        if (!parts.length || parts.includes('..')) return null;\r\n        const normalized = parts.join('/');\r\n        return /^[a-z][a-z\\d+.-]*:/i.test(normalized) ? null : normalized;\r\n    }\r\n    function validateOptions({ changedFiles = [], highlightChanges = true } = {}) {\r\n        if (!Array.isArray(changedFiles) || changedFiles.length > MAX_FILES) throw new TypeError(`changedFiles must be an array of at most ${MAX_FILES} repository-relative file paths.`);\r\n        if (typeof highlightChanges !== 'boolean') throw new TypeError('highlightChanges must be a boolean.');\r\n        const paths = new Set();\r\n        for (const value of changedFiles) {\r\n            const path = normalizePath(value);\r\n            if (!path) throw new TypeError('Each changedFiles entry must be a repository-relative file path (1–4096 characters), without a drive, URL, control characters or .. segments.');\r\n            paths.add(path);\r\n        }\r\n        return { changedFiles: [...paths], highlightChanges };\r\n    }\r\n    function match(index, files) {\r\n        const { changedFiles } = validateOptions({ changedFiles: files ?? null });\r\n        const wanted = new Set(changedFiles), found = new Set(), paths = new Map(), nodeIds = [];\r\n        function pathFor(node) {\r\n            if (paths.has(node.id)) return paths.get(node.id);\r\n            // A supplied path is authoritative, even if unsupported. Only missing paths inherit.\r\n            const path = node.path !== undefined ? normalizePath(node.path, true)\r\n                : node.parentId ? pathFor(index.nodes.get(node.parentId)) : null;\r\n            paths.set(node.id, path); return path;\r\n        }\r\n        for (const node of index.nodes.values()) {\r\n            const path = pathFor(node);\r\n            if (wanted.has(path)) { found.add(path); nodeIds.push(node.id); }\r\n        }\r\n        return { changedFiles, matchedFiles: changedFiles.filter(path => found.has(path)),\r\n            unmatchedFiles: changedFiles.filter(path => !found.has(path)), nodeIds };\r\n    }\r\n    root.CodeAtlasChanges = Object.freeze({ normalizePath, validateOptions, match });\r\n})(globalThis);\r\n\u003c/script>\r\n\u003cscript>(function (root) {\n    'use strict';\n    const GROUP_KINDS = new Set(['module', 'namespace', 'directory', 'package', 'service']);\n    const TYPE_KINDS = new Set(['class', 'interface', 'struct', 'type']);\n    function hash(text) {\n        let value = 2166136261;\n        for (const char of text) value = Math.imul(value ^ char.charCodeAt(0), 16777619);\n        return (value >>> 0) / 4294967296;\n    }\n    function family(node) {\n        if (GROUP_KINDS.has(node.kind)) return 'module';\n        if (TYPE_KINDS.has(node.kind)) return 'class';\n        if (['table', 'view', 'database', 'procedure'].includes(node.kind)) return 'data';\n        if (node.kind === 'file') return 'file';\n        return 'function';\n    }\n    function scopeNodes(graph, index, scope, filter = 'all', limit = 700) {\n        const inScope = node => {\n            if (scope === null) return true;\n            let parent = node.parentId;\n            while (parent) { if (parent === scope) return true; parent = index.nodes.get(parent)?.parentId; }\n            return false;\n        };\n        const eligible = graph.nodes.filter(node => inScope(node) && (filter === 'all' || family(node) === 'module'\n            || (filter === 'classes' ? family(node) === 'class' : filter === 'files' ? family(node) === 'file' : family(node) === 'data')));\n        // Large repositories start at their real directory children. Search/focus can\n        // still reveal any file, while smaller snapshots retain the approved constellation.\n        if (eligible.length > 200) {\n            const children = eligible.filter(node => (node.parentId ?? null) === scope);\n            if (children.length && children.length < eligible.length)\n                return { nodes: children.slice(0, limit), total: eligible.length };\n        }\n        // Keep groups and types before leaf symbols when bounding a large snapshot.\n        const priority = node => ({ module: 0, class: 1, data: 2, file: 3, function: 4 })[family(node)];\n        const visible = eligible.length > limit ? [...eligible].sort((a, b) => priority(a) - priority(b)).slice(0, limit) : eligible;\n        return { nodes: visible, total: eligible.length };\n    }\n    function layout(nodes, edges, index) {\n        const visible = new Set(nodes.map(n => n.id));\n        const top = node => {\n            let current = node;\n            while (current.parentId && visible.has(current.parentId)) current = index.nodes.get(current.parentId);\n            // Filtered views may have omitted file parents. Walk through those too.\n            let ancestor = current;\n            while (ancestor.parentId) {\n                ancestor = index.nodes.get(ancestor.parentId);\n                if (visible.has(ancestor.id)) current = ancestor;\n            }\n            return current.id;\n        };\n        const groups = [...new Set(nodes.map(top))];\n        const groupCount = groups.length;\n        const centers = groups.map((id, i) => {\n            const slot = i, angle = slot * 2.399963229728653;\n            const radius = groupCount === 1 ? 0 : 165 * Math.sqrt(slot + .7);\n            return { id, x: 600 + Math.cos(angle) * radius * 1.3, y: 400 + Math.sin(angle) * radius * .8 };\n        });\n        const centerById = new Map(centers.map(c => [c.id, c]));\n        const positions = new Map();\n        function seed(node) {\n            if (positions.has(node.id)) return positions.get(node.id);\n            const group = top(node), center = centerById.get(group);\n            const parent = node.parentId ? index.nodes.get(node.parentId) : null;\n            const base = parent && visible.has(parent.id) ? seed(parent) : center;\n            const angle = hash(node.id) * Math.PI * 2;\n            const radius = node.id === group ? 0 : family(node) === 'file' ? 95 + hash(node.id + 'r') * 70 : 35 + hash(node.id + 'r') * 35;\n            const item = { id: node.id, group, node, x: base.x + Math.cos(angle) * radius, y: base.y + Math.sin(angle) * radius,\n                vx: 0, vy: 0, z: hash(node.id + 'depth'), phase: hash(group) * 6.28, label: family(node) === 'module' };\n            positions.set(node.id, item); return item;\n        }\n        const items = nodes.map(seed);\n        const springs = edges.filter(e => e.source !== e.target && visible.has(e.source) && visible.has(e.target))\n            .map(e => ({ a: positions.get(e.source), b: positions.get(e.target), structural: e.structural }));\n        const iterations = items.length \u003c 400 ? 190 : 130;\n        for (let iteration = 0; iteration \u003c iterations; iteration++) {\n            const cooling = 1 - iteration / iterations * .85;\n            for (let i = 0; i \u003c items.length; i++) {\n                const a = items[i];\n                for (let j = i + 1; j \u003c items.length; j++) {\n                    const b = items[j];\n                    let dx = a.x - b.x, dy = a.y - b.y;\n                    if (dx === 0 && dy === 0) { dx = .2 + hash(a.id); dy = .2 + hash(b.id); }\n                    const d2 = dx * dx + dy * dy, d = Math.sqrt(d2);\n                const magnitude = Math.min(6, 460 / Math.max(d2, 16)) + (d \u003c 34 ? (34 - d) * .22 : 0);\n                    const fx = dx / d * magnitude, fy = dy / d * magnitude;\n                    a.vx += fx; a.vy += fy; b.vx -= fx; b.vy -= fy;\n                }\n            }\n            for (const { a, b, structural } of springs) {\n                const dx = b.x - a.x, dy = b.y - a.y, d = Math.hypot(dx, dy) || 1;\n                const sameGroup = a.group === b.group;\n                const target = family(a.node) === 'module' ? 125 : structural ? 42 : 66;\n                const force = (d - target) * (sameGroup ? .019 : .003);\n                const fx = dx / d * force, fy = dy / d * force;\n                a.vx += fx; a.vy += fy; b.vx -= fx; b.vy -= fy;\n            }\n            for (const item of items) {\n                const center = centerById.get(item.group);\n                const anchor = item.id === item.group ? .11 : .006;\n                item.vx = (item.vx + (center.x - item.x) * anchor) * .72;\n                item.vy = (item.vy + (center.y - item.y) * anchor) * .72;\n                item.x += Math.max(-10, Math.min(10, item.vx)) * cooling;\n                item.y += Math.max(-10, Math.min(10, item.vy)) * cooling;\n            }\n        }\n        // A wide field makes better use of a desktop viewport than a tall cluster.\n        if (items.length > 50) for (const item of items) {\n            item.x = 600 + (item.x - 600) * 1.22;\n            item.y = 400 + (item.y - 400) * .97;\n        }\n        return positions;\n    }\n    root.CodeAtlasLayout = Object.freeze({ family, scopeNodes, layout, hash });\n})(globalThis);\n\u003c/script>\r\n\u003cscript>(function (root) {\n    'use strict';\n    // World depth is a stable visual arrangement, not a code metric or dependency distance.\n    const origin = { x: 600, y: 400, z: 0 };\n    const front = { yaw: 0, pitch: 0 };\n    const clamp = (value, low, high) => Math.max(low, Math.min(high, value));\n    function position(item, time = 0, pointer = { x: 0, y: 0 }) {\n        const depth = item.z ?? .5;\n        const phase = item.phase + depth * 17;\n        const groupDepth = .5 + Math.sin(item.phase) * .5;\n        // Shared sway keeps clusters together; small independent waves make nodes\n        // feel suspended. Subtract the initial phase to preserve the layout at t=0.\n        const sway = (period, offset) => Math.sin(time / period + offset) - Math.sin(offset);\n        const parallax = 3 + groupDepth * 3 + depth;\n        return {\n            x: item.x\n                + sway(2600, item.phase) * (3.5 + groupDepth * 1.5)\n                + sway(1500, phase) * 1.1 + sway(420, phase * 1.7) * .35 + pointer.x * parallax,\n            y: item.y\n                + sway(3100, item.phase + Math.PI / 2) * (2.5 + groupDepth * 1.2)\n                + sway(1700, phase + .8) * .9 + sway(530, phase * 1.3) * .3 + pointer.y * parallax,\n            z: Math.sin(item.phase) * 220 + (depth - .5) * 180 + sway(1900, phase + 1.2) * 1.4\n        };\n    }\n    // Orthographic camera: yaw about Y, then pitch about X. Labels stay screen-facing.\n    function projectPoint(point, orbit = front, pivot = origin) {\n        const x = point.x - pivot.x, y = point.y - pivot.y, z = point.z - pivot.z;\n        const cy = Math.cos(orbit.yaw), sy = Math.sin(orbit.yaw);\n        const cp = Math.cos(orbit.pitch), sp = Math.sin(orbit.pitch);\n        const turnedX = x * cy + z * sy, turnedZ = -x * sy + z * cy;\n        const depth = y * sp + turnedZ * cp;\n        return { x: pivot.x + turnedX, y: pivot.y + y * cp - turnedZ * sp, depth,\n            brightness: clamp(.82 + depth / 2400, .58, 1), scale: clamp(1 + depth / 2200, .72, 1.28) };\n    }\n    // Hold camera depth while a node is dragged, then store its position in world space.\n    function unproject(point, orbit = front, pivot = origin) {\n        const x = point.x - pivot.x, y = point.y - pivot.y;\n        const cy = Math.cos(orbit.yaw), sy = Math.sin(orbit.yaw);\n        const cp = Math.cos(orbit.pitch), sp = Math.sin(orbit.pitch);\n        const worldY = y * cp + point.depth * sp, turnedZ = -y * sp + point.depth * cp;\n        return { x: pivot.x + x * cy - turnedZ * sy, y: pivot.y + worldY, z: pivot.z + x * sy + turnedZ * cy };\n    }\n    function center(items) {\n        if (!items.length) return { ...origin };\n        const points = items.map(item => position(item));\n        return Object.fromEntries(['x', 'y', 'z'].map(axis => [axis,\n            (Math.min(...points.map(p => p[axis])) + Math.max(...points.map(p => p[axis]))) / 2]));\n    }\n    function project(item, time, pointer, orbit, pivot) {\n        return projectPoint(position(item, time, pointer), orbit, pivot);\n    }\n    root.CodeAtlasSpace = Object.freeze({ position, projectPoint, unproject, center, project });\n})(globalThis);\n\u003c/script>\r\n\u003cscript>(function (root) {\r\n    'use strict';\r\n    // Match VibeRails' semantic CSS variable names; graph-specific tokens stay separate.\r\n    const tokens = {\r\n        colors: { background: '--color-bg-base', surface: '--color-bg-surface', surfaceHover: '--color-bg-surface-hover',\r\n            elevated: '--color-bg-elevated', primary: '--color-primary', primaryHover: '--color-primary-dark',\r\n            onPrimary: '--color-on-primary', accent: '--color-accent', text: '--color-text', muted: '--color-text-muted',\r\n            border: '--color-border', success: '--color-success', warning: '--color-warning', danger: '--color-danger', shadow: '--color-shadow' },\r\n        nodes: { module: '--node-module', class: '--node-class', file: '--node-file', function: '--node-function', data: '--node-data' },\r\n        graph: { edge: '--graph-edge', crossEdge: '--graph-cross-edge', changed: '--graph-changed' }\r\n    };\r\n    const palettes = {\r\n        dark: {\r\n            colors: { background: '#101319', surface: '#171c26', surfaceHover: '#263247', elevated: '#202733',\r\n                primary: '#a6b7ed', primaryHover: '#bac8f3', onPrimary: '#172238', accent: '#78dce8', text: '#e3e8ef',\r\n                muted: '#99a5b8', border: '#2d3543', success: '#46c9b0', warning: '#f2bd72', danger: '#ed939e', shadow: '#000000' },\r\n            nodes: { module: '#9b8afb', class: '#a9b8ff', file: '#62b0f5', function: '#46c9b0', data: '#78dce8' },\r\n            graph: { edge: '#93a4be', crossEdge: '#b2a5d4', changed: '#b5ecf4' }\r\n        },\r\n        light: {\r\n            colors: { background: '#f4f7fc', surface: '#ffffff', surfaceHover: '#dfe8f5', elevated: '#e8eef7',\r\n                primary: '#395bc3', primaryHover: '#2b469c', onPrimary: '#ffffff', accent: '#08798b', text: '#1b2940',\r\n                muted: '#54647d', border: '#c6d2e3', success: '#14755d', warning: '#946100', danger: '#c03342', shadow: '#000000' },\r\n            nodes: { module: '#7454c4', class: '#455fc5', file: '#2770b0', function: '#18816f', data: '#167a8a' },\r\n            graph: { edge: '#61748e', crossEdge: '#8872a4', changed: '#0b7d91' }\r\n        }\r\n    };\r\n    function record(value, name, allowed) {\r\n        if (!value || typeof value !== 'object' || Array.isArray(value)) throw new TypeError(`${name} must be an object.`);\r\n        for (const key of Object.keys(value)) if (!allowed.includes(key)) throw new TypeError(`Unknown ${name} option: ${key}`);\r\n    }\r\n    function color(value, name) {\r\n        if (typeof value !== 'string' || !/^#(?:[\\da-f]{3}|[\\da-f]{4}|[\\da-f]{6}|[\\da-f]{8})$/i.test(value)) {\r\n            throw new TypeError(`${name} must be a hex color (#RGB, #RGBA, #RRGGBB or #RRGGBBAA).`);\r\n        }\r\n        const digits = value.slice(1).toLowerCase();\r\n        return '#' + (digits.length \u003c= 4 ? [...digits].map(c => c + c).join('') : digits);\r\n    }\r\n    function resolve(theme = {}) {\r\n        record(theme, 'theme', ['mode', ...Object.keys(tokens)]);\r\n        const mode = theme.mode === undefined ? 'dark' : theme.mode;\r\n        if (!['dark', 'light'].includes(mode)) throw new TypeError('theme.mode must be dark or light.');\r\n        const result = { mode };\r\n        for (const [group, properties] of Object.entries(tokens)) {\r\n            const overrides = theme[group] === undefined ? {} : theme[group];\r\n            record(overrides, `theme.${group}`, Object.keys(properties));\r\n            result[group] = { ...palettes[mode][group] };\r\n            for (const key of Object.keys(overrides)) result[group][key] = color(overrides[key], `theme.${group}.${key}`);\r\n        }\r\n        return result;\r\n    }\r\n    function toCss(theme) {\r\n        const resolved = resolve(theme);\r\n        const declarations = [`color-scheme:${resolved.mode}`];\r\n        for (const [group, properties] of Object.entries(tokens)) {\r\n            for (const [key, variable] of Object.entries(properties)) declarations.push(`${variable}:${resolved[group][key]}`);\r\n        }\r\n        return `:root{${declarations.join(';')}}`;\r\n    }\r\n    for (const group of Object.values(tokens)) Object.freeze(group);\r\n    root.CodeAtlasTheme = Object.freeze({ resolve, toCss, color, tokens: Object.freeze(tokens) });\r\n})(globalThis);\r\n\u003c/script>\r\n\u003cscript>(function (root) {\n    'use strict';\n    // Paint a small, independent visual before doing the synchronous layout work.\n    // The graph itself stays still until its viewport has settled and the fade ends.\n    function start({ initialize, viewport, settle, onReady, onError, motionQuery }) {\n        const atlas = document.getElementById('atlas'), loader = document.getElementById('atlas-loader');\n        const began = performance.now();\n        let initialized = false, complete = false, phase = 'loading', timer, paintFrame;\n        let lastSize = '', stableSince = began, preparedAt = began, animations = [];\n        let resolveReady, rejectReady;\n        const ready = new Promise((resolve, reject) => { resolveReady = resolve; rejectReady = reject; });\n        ready.catch(() => {}); // Standalone documents have no host waiting on this promise.\n        function cleanup() {\n            clearTimeout(timer); cancelAnimationFrame(paintFrame);\n            document.removeEventListener('visibilitychange', skipMotion);\n            motionQuery.removeEventListener('change', skipMotion);\n            for (const animation of animations) animation.cancel();\n            animations = [];\n        }\n        function finish() {\n            if (complete) return;\n            complete = true; cleanup();\n            document.body.dataset.startup = 'ready';\n            loader.remove(); atlas.inert = false; atlas.setAttribute('aria-busy', 'false');\n            onReady(); resolveReady();\n        }\n        function fail(error) {\n            if (complete) return;\n            complete = true; cleanup();\n            document.body.dataset.startup = 'error';\n            loader.querySelector('.loading-caption').textContent = 'Couldn’t prepare this graph';\n            loader.querySelector('.loading-description').textContent = error.message;\n            atlas.setAttribute('aria-busy', 'false');\n            onError(error); rejectReady(error);\n        }\n        function skipMotion() {\n            if (complete || !initialized || !document.hidden && !motionQuery.matches) return;\n            settle(); finish();\n        }\n        function reveal() {\n            settle();\n            const { width, height } = viewport();\n            if (document.hidden || motionQuery.matches || !width || !height) { finish(); return; }\n            phase = 'revealing'; document.body.dataset.startup = phase;\n            const options = { duration: 620, easing: 'cubic-bezier(.4,0,.2,1)', fill: 'both' };\n            animations = [atlas.animate([{ opacity: 0 }, { opacity: 1 }], options),\n                loader.animate([{ opacity: 1 }, { opacity: 0 }], options)];\n            // A bounded timer is a fallback for background/disabled animation timelines.\n            Promise.all(animations.map(animation => animation.finished)).then(finish, () => {});\n            timer = setTimeout(finish, 750);\n        }\n        function check() {\n            if (complete || phase !== 'loading') return;\n            const now = performance.now(), { width, height } = viewport(), size = `${width},${height}`;\n            if (size !== lastSize) { lastSize = size; stableSince = now; }\n            if (document.hidden || motionQuery.matches || !width || !height\n                || now - began >= 750 && (now - stableSince >= 140 || now - preparedAt >= 2500)) {\n                reveal(); return;\n            }\n            timer = setTimeout(check, 40);\n        }\n        function prepare() {\n            if (initialized || complete) return;\n            clearTimeout(timer); cancelAnimationFrame(paintFrame);\n            try {\n                initialize(); initialized = true; preparedAt = performance.now(); check();\n            } catch (error) { fail(error); }\n        }\n        document.addEventListener('visibilitychange', skipMotion);\n        motionQuery.addEventListener('change', skipMotion);\n        if (motionQuery.matches || document.hidden) prepare();\n        else {\n            paintFrame = requestAnimationFrame(() => { paintFrame = requestAnimationFrame(prepare); });\n            // rAF can be suspended in hidden/zero-size frames. Never deadlock a host awaiting ready.\n            timer = setTimeout(prepare, 80);\n        }\n        return Object.freeze({ ready });\n    }\n    root.CodeAtlasStartup = Object.freeze({ start });\n})(globalThis);\n\u003c/script>\r\n\u003cscript>(function () {\r\n    'use strict';\r\n    const C = globalThis.CodeAtlasContract;\r\n    const L = globalThis.CodeAtlasLayout;\r\n    const S = globalThis.CodeAtlasSpace;\r\n    const Changes = globalThis.CodeAtlasChanges;\r\n    const Theme = globalThis.CodeAtlasTheme;\r\n    let currentTheme;\r\n    const $ = id => document.getElementById(id);\r\n    const svgNS = 'http://www.w3.org/2000/svg';\r\n    const motionQuery = matchMedia('(prefers-reduced-motion: reduce)');\r\n    const state = { graph: null, index: null, scope: null, selected: null,\r\n        zoom: 1, panX: 0, panY: 0, layout: [], edges: [], frame: 0, time: 0, last: 0, dialogTab: 'overview',\r\n        display: 'constellation', filter: 'all', overviewScope: null, layoutCache: new Map(),\r\n        pointer: { x: 0, y: 0 }, pointerTarget: { x: 0, y: 0 }, hovered: null,\r\n        pinned: new Map(), overviewCamera: null, reveal: null, navigation: [], detailId: null, searchText: '',\r\n        orbit: { yaw: 0, pitch: 0 }, pivot: { x: 600, y: 400, z: 0 }, interaction: 'pan',\r\n        transition: null, inertia: null, starting: true, restoring: false, animations: new Set(),\r\n        query: '', matches: null, viewSummary: null, changes: null, changedIds: new Set(), highlightChanges: true };\r\n    const MAX_VISIBLE = 32;\r\n    let toastTimer, rotateTimer, drag, nodeDrag, suppressClick = false, previousFocus, onOpenDetails = null;\r\n    const rotationKeys = new Set();\r\n    function el(tag, className, text) {\r\n        const node = document.createElement(tag);\r\n        if (className) node.className = className;\r\n        if (text !== undefined) node.textContent = String(text);\r\n        return node;\r\n    }\r\n    function button(text, className, action) {\r\n        const node = el('button', className, text); node.type = 'button';\r\n        node.addEventListener('click', action); return node;\r\n    }\r\n    function svg(tag, attributes) {\r\n        const node = document.createElementNS(svgNS, tag);\r\n        for (const [key, value] of Object.entries(attributes ?? {})) node.setAttribute(key, value);\r\n        return node;\r\n    }\r\n    function toast(message, error = false) {\r\n        clearTimeout(toastTimer); $('toast').textContent = message;\r\n        $('toast').classList.toggle('error', error); $('toast').hidden = false;\r\n        toastTimer = setTimeout(() => { $('toast').hidden = true; }, error ? 8000 : 3500);\r\n    }\r\n    function category(node) {\r\n        return { module: { color: 'var(--node-module)', icon: '◇' }, class: { color: 'var(--node-class)', icon: '⬡' },\r\n            data: { color: 'var(--node-data)', icon: '▤' }, file: { color: 'var(--node-file)', icon: '▱' },\r\n            function: { color: 'var(--node-function)', icon: 'ƒ' } }[L.family(node)];\r\n    }\r\n    function constellation() { return state.display === 'constellation'; }\r\n    const pinKey = id => JSON.stringify([state.scope, state.filter, id]);\r\n    const pinFor = id => state.pinned.get(pinKey(id));\r\n    const count = value => value === undefined ? '—' : new Intl.NumberFormat('en', { maximumFractionDigits: 1 }).format(value);\r\n    function childNodes(id) { return state.index.children.get(id) ?? []; }\r\n    function related(id) {\r\n        const direct = state.index.adjacency.get(id) ?? [];\r\n        return direct.map(edge => ({ node: state.index.nodes.get(edge.source === id ? edge.target : edge.source),\r\n            label: `${edge.source === id ? 'Outgoing' : 'Incoming'} · ${edge.kind}`, direction: edge.source === id ? '↗' : '↙' }));\r\n    }\r\n    function loadGraph(graph) {\r\n        const index = C.index(graph); // Validate before replacing the working graph.\r\n        stopChoreography(); finishReveals();\r\n        state.graph = graph; state.index = index; state.scope = null; state.selected = null;\r\n        state.overviewScope = null; state.layoutCache.clear(); state.filter = 'all'; $('entity-filter').value = 'all';\r\n        state.pinned.clear(); state.overviewCamera = null; state.reveal = null;\r\n        state.orbit = { yaw: 0, pitch: 0 };\r\n        state.query = ''; state.matches = null;\r\n        for (const kind of ['module', 'class', 'file', 'function', 'data']) $('count-' + kind).textContent = graph.nodes.filter(n => L.family(n) === kind).length;\r\n        $('search').value = ''; $('search-results').hidden = true; $('clear-search').hidden = true;\r\n        $('search').closest('.search-wrap').classList.remove('has-query');\r\n        updateBack(); render();\r\n    }\r\n    function captureView() {\r\n        return { scope: state.scope, selected: state.selected, display: state.display, filter: state.filter,\r\n            reveal: state.reveal, overviewScope: state.overviewScope, overviewCamera: state.overviewCamera ? { ...state.overviewCamera } : null,\r\n            searchText: state.searchText, orbit: { ...state.orbit }, camera: cameraState(),\r\n            detailId: state.detailId, dialogTab: state.dialogTab,\r\n            viewport: { width: $('stage').clientWidth, height: $('stage').clientHeight } };\r\n    }\r\n    function updateBack() {\r\n        $('back-button').disabled = state.navigation.length === 0;\r\n        if ($('dialog-back')) $('dialog-back').disabled = state.navigation.length === 0;\r\n    }\r\n    function rememberView() {\r\n        state.navigation.push(captureView());\r\n        if (state.navigation.length > 100) state.navigation.shift();\r\n        updateBack();\r\n    }\r\n    function navigateBack() {\r\n        const view = state.navigation.pop(); if (!view) return;\r\n        stopChoreography(); finishReveals();\r\n        previousFocus = null;\r\n        if ($('details-dialog').open) $('details-dialog').close();\r\n        const { camera, viewport, ...values } = view;\r\n        Object.assign(state, values);\r\n        $('entity-filter').value = state.filter;\r\n        setSearchValue(state.searchText);\r\n        state.restoring = true; render(); state.restoring = false; stopChoreography(); finishReveals();\r\n        if (viewport.width === $('stage').clientWidth && viewport.height === $('stage').clientHeight) Object.assign(state, camera);\r\n        else fitGraph();\r\n        applyCamera(); draw(0); updateBack();\r\n        if (state.detailId) {\r\n            renderDetails(state.detailId); $('details-dialog').showModal();\r\n        } else {\r\n            const focus = state.layout.find(item => item.id === state.selected)?.card;\r\n            (focus ?? ($('back-button').disabled ? $('stage') : $('back-button'))).focus({ preventScroll: true });\r\n            $('node-tooltip').hidden = true;\r\n        }\r\n    }\r\n    function goScope(id, record = true) {\r\n        if (record && (state.scope !== id || state.selected !== null)) rememberView();\r\n        stopChoreography();\r\n        state.scope = id; state.overviewScope = id; state.selected = null; state.overviewCamera = null; state.reveal = null;\r\n        render();\r\n    }\r\n    function focusNode(id, record = true) {\r\n        if (!state.index.nodes.has(id)) return;\r\n        if (record && state.selected !== id) rememberView();\r\n        stopChoreography(); finishReveals();\r\n        // Following a relationship explicitly reveals its target, even outside the current query.\r\n        if (state.query && !state.matches.has(id)) clearSearch(false);\r\n        if (constellation()) {\r\n            if (!state.selected) state.overviewCamera = { zoom: state.zoom, panX: state.panX, panY: state.panY };\r\n            if (!state.layout.some(item => item.id === id)) {\r\n                state.scope = state.index.nodes.get(id).parentId ?? null;\r\n                state.filter = 'all'; $('entity-filter').value = 'all'; state.reveal = id;\r\n                renderGraph();\r\n                stopChoreography(); finishReveals();\r\n            }\r\n            state.selected = id;\r\n            $('search-results').hidden = true;\r\n            renderBreadcrumbs(); renderInspector();\r\n            for (const item of state.layout) {\r\n                item.card.classList.toggle('selected', item.id === id);\r\n                item.card.setAttribute('aria-pressed', String(item.id === id));\r\n            }\r\n            highlight(id);\r\n            $('view-hint').hidden = true;\r\n            const item = state.layout.find(item => item.id === id);\r\n            if (item) {\r\n                const point = item.lastPosition ?? projectItem(item);\r\n                const zoom = Math.max(state.zoom, .85);\r\n                const width = $('stage').clientWidth, height = $('stage').clientHeight;\r\n                animateCamera({ zoom, panX: (width > 760 ? (width - 345) / 2 : width / 2) - point.x * zoom,\r\n                    panY: height * .46 - point.y * zoom });\r\n                item.card.focus({ preventScroll: true }); $('node-tooltip').hidden = true;\r\n            }\r\n            return;\r\n        }\r\n        state.selected = id;\r\n        state.scope = state.index.nodes.get(id).parentId ?? null;\r\n        $('search-results').hidden = true; render();\r\n        state.layout.find(item => item.id === id)?.card.focus({ preventScroll: true });\r\n    }\r\n    function renderBreadcrumbs() {\r\n        const root = button('⌘  ' + state.graph.repository.name, 'crumb', () => goScope(null));\r\n        const nodes = [];\r\n        let current = state.selected ?? state.scope;\r\n        while (current) { const n = state.index.nodes.get(current); nodes.unshift(n); current = n.parentId; }\r\n        const fragment = document.createDocumentFragment(); fragment.append(root);\r\n        for (const node of nodes) {\r\n            fragment.append(el('span', '', '/'), button(node.name, 'crumb', () => childNodes(node.id).length ? goScope(node.id) : focusNode(node.id)));\r\n        }\r\n        $('breadcrumbs').replaceChildren(fragment);\r\n    }\r\n    function render() {\r\n        state.hovered = null;\r\n        $('node-tooltip').hidden = true;\r\n        $('stage').classList.toggle('constellation', constellation());\r\n        $('constellation-button').setAttribute('aria-pressed', String(state.display === 'constellation'));\r\n        $('cards-button').setAttribute('aria-pressed', String(state.display === 'cards'));\r\n        $('entity-filter').disabled = !constellation();\r\n        if (!constellation()) { clearTimeout(rotateTimer); rotationKeys.clear(); state.interaction = 'pan'; }\r\n        updateGestures();\r\n        $('view-hint').hidden = !!state.selected;\r\n        $('view-hint').textContent = constellation() ? 'Hover a node to trace its connections' : 'Explore one level of the hierarchy';\r\n        renderBreadcrumbs(); renderInspector(); renderGraph();\r\n        highlight(state.selected);\r\n        $('view-label').textContent = state.scope ? state.index.nodes.get(state.scope).name.toUpperCase() : constellation() ? 'REPOSITORY GRAPH' : 'REPOSITORY OVERVIEW';\r\n    }\r\n    function visibleGraph() {\r\n        if (constellation()) {\r\n            const view = L.scopeNodes(state.graph, state.index, state.scope, state.filter);\r\n            if (state.reveal && !view.nodes.some(n => n.id === state.reveal)) {\r\n                if (view.nodes.length >= 700) view.nodes.pop();\r\n                view.nodes.push(state.index.nodes.get(state.reveal));\r\n            }\r\n            const visible = new Set(view.nodes.map(n => n.id));\r\n            view.edges = C.aggregateEdges(state.graph, visible, state.index.nodes);\r\n            const pairs = new Set(view.edges.map(e => JSON.stringify([e.source, e.target])));\r\n            for (const node of view.nodes) {\r\n                let parent = node.parentId;\r\n                while (parent && !visible.has(parent)) parent = state.index.nodes.get(parent)?.parentId;\r\n                if (parent && !pairs.has(JSON.stringify([parent, node.id]))) {\r\n                    view.edges.push({ source: parent, target: node.id, kind: 'contains', count: 1, structural: true });\r\n                }\r\n            }\r\n            return view;\r\n        }\r\n        if (!state.selected) {\r\n            const all = childNodes(state.scope), nodes = all.slice(0, MAX_VISIBLE);\r\n            return { nodes, total: all.length, edges: C.aggregateEdges(state.graph, nodes.map(n => n.id), state.index.nodes) };\r\n        }\r\n        const center = state.index.nodes.get(state.selected);\r\n        const neighbors = new Map();\r\n        for (const item of related(center.id)) if (item.node.id !== center.id) neighbors.set(item.node.id, item.node);\r\n        for (const child of childNodes(center.id)) neighbors.set(child.id, child);\r\n        if (center.parentId) neighbors.set(center.parentId, state.index.nodes.get(center.parentId));\r\n        const nodes = [center, ...[...neighbors.values()].slice(0, MAX_VISIBLE - 1)];\r\n        const ids = new Set(nodes.map(n => n.id));\r\n        const edges = state.graph.edges.filter(e => ids.has(e.source) && ids.has(e.target)).map(e => ({ ...e, count: 1 }));\r\n        for (const n of nodes) {\r\n            if (n.parentId && ids.has(n.parentId) && !edges.some(e => e.source === n.parentId && e.target === n.id)) {\r\n                edges.push({ source: n.parentId, target: n.id, kind: 'contains', count: 1, structural: true });\r\n            }\r\n        }\r\n        return { nodes, total: neighbors.size + 1, edges };\r\n    }\r\n    function makeCard(node, anchor = false) {\r\n        const selected = node?.id === state.selected;\r\n        const card = button('', `node${anchor ? ' root-node' : ''}${selected ? ' selected' : ''}`, () => {\r\n            if (suppressClick) return;\r\n            if (anchor) { if (state.scope) focusNode(state.scope); }\r\n            else focusNode(node.id);\r\n        });\r\n        card.dataset.nodeId = node?.id ?? '';\r\n        if (anchor) {\r\n            card.append(el('span', 'root-glyph', '✳'), el('span', 'node-name', state.scope ? state.index.nodes.get(state.scope).name : state.graph.repository.name), el('span', 'node-meta', 'REPOSITORY / ' + (state.scope ? 'SCOPE' : 'ROOT')));\r\n            card.setAttribute('aria-label', state.scope ? 'Focus current scope' : 'Repository root');\r\n        } else {\r\n            const visual = category(node);\r\n            card.style.setProperty('--node-color', visual.color);\r\n            const top = el('span', 'node-top');\r\n            top.append(el('span', 'node-symbol', visual.icon), el('span', '', node.kind), el('span', 'node-expand', '↗'));\r\n            const meta = el('span', 'node-meta');\r\n            meta.append(el('span', '', node.language ?? 'Group'));\r\n            const children = childNodes(node.id).length;\r\n            meta.append(el('span', '', children ? `${children} entities` : `${state.index.adjacency.get(node.id).length} links`));\r\n            card.append(top, el('span', 'node-name', node.name), meta);\r\n            card.title = `${node.name}\\n${node.path ?? node.kind}\\nSelect to explore connections`;\r\n            card.setAttribute('aria-label', `${node.kind}: ${node.name}`);\r\n            card.setAttribute('aria-pressed', String(selected));\r\n            card.addEventListener('mouseenter', () => highlight(node.id));\r\n            card.addEventListener('mouseleave', () => highlight(null));\r\n            card.addEventListener('focus', () => highlight(node.id));\r\n            card.addEventListener('blur', () => highlight(null));\r\n        }\r\n        return card;\r\n    }\r\n    function makeOrb(node) {\r\n        const family = L.family(node);\r\n        const card = button('', `node orb ${family}${state.selected === node.id ? ' selected' : ''}`, () => { if (!suppressClick) focusNode(node.id); });\r\n        card.dataset.nodeId = node.id;\r\n        card.style.setProperty('--node-color', category(node).color);\r\n        card.setAttribute('aria-label', `${node.kind}: ${node.name}`);\r\n        card.setAttribute('aria-pressed', String(state.selected === node.id));\r\n        card.append(el('span', 'orb-shape'), el('span', 'orb-pulse'), el('span', 'node-name', node.name));\r\n        const show = () => {\r\n            if (drag || nodeDrag) return;\r\n            state.hovered = node.id;\r\n            highlight(node.id);\r\n            const tip = $('node-tooltip');\r\n            tip.replaceChildren(el('div', 'tooltip-kind', `${node.kind} · ${node.language ?? 'Group'}`), el('strong', '', node.name),\r\n                el('div', 'tooltip-path', node.path ?? node.summary ?? 'Select to explore this entity'),\r\n                el('div', 'tooltip-action', `${state.index.adjacency.get(node.id).length} direct links · Click to explore ↗`));\r\n            const box = card.getBoundingClientRect(), stage = $('stage').getBoundingClientRect();\r\n            tip.style.left = `${Math.max(10, Math.min(stage.width - 260, box.x - stage.x + 24))}px`;\r\n            tip.style.top = `${Math.max(42, Math.min(stage.height - 140, box.y - stage.y - 32))}px`;\r\n            tip.hidden = false;\r\n        };\r\n        const hide = () => { state.hovered = null; highlight(state.selected); $('node-tooltip').hidden = true; };\r\n        card.addEventListener('mouseenter', show); card.addEventListener('mouseleave', hide);\r\n        card.addEventListener('focus', show); card.addEventListener('blur', hide);\r\n        return card;\r\n    }\r\n    function renderGraph() {\r\n        cancelAnimationFrame(state.frame);\r\n        stopChoreography(); finishReveals();\r\n        const view = visibleGraph();\r\n        $('nodes').replaceChildren(); $('edge-layer').replaceChildren();\r\n        state.layout = []; state.edges = [];\r\n        const compact = constellation();\r\n        const hasCenter = !compact && !state.selected && view.nodes.length > 0;\r\n        const cacheKey = JSON.stringify([state.scope, state.filter, state.reveal]);\r\n        if (compact && !state.layoutCache.has(cacheKey)) state.layoutCache.set(cacheKey, L.layout(view.nodes, view.edges.slice(0, 2500), state.index));\r\n        const positions = compact ? state.layoutCache.get(cacheKey) : null;\r\n        if (hasCenter) {\r\n            const card = makeCard(null, true); $('nodes').append(card);\r\n            state.layout.push({ id: null, card, x: 600, y: 370, width: 154, height: 110, phase: 0 });\r\n        }\r\n        const outer = state.selected ? view.nodes.slice(1) : view.nodes;\r\n        for (let i = 0; i \u003c view.nodes.length; i++) {\r\n            const node = view.nodes[i], center = state.selected === node.id;\r\n            if (compact) {\r\n                const card = makeOrb(node), position = positions.get(node.id); $('nodes').append(card);\r\n                card.style.setProperty('--depth', position.z.toFixed(2));\r\n                state.layout.push({ ...position, card, width: 28, height: 28, compact: true });\r\n                continue;\r\n            }\r\n            const slot = state.selected ? i - 1 : i;\r\n            const ring = Math.floor(Math.max(0, slot) / 10);\r\n            const ringCount = Math.min(10, outer.length - ring * 10);\r\n            const angle = -Math.PI / 2 + 2 * Math.PI * (slot % 10) / Math.max(1, ringCount) + (ring % 2 ? .25 : 0);\r\n            const radiusX = 365 + ring * 270, radiusY = 235 + ring * 175;\r\n            const card = makeCard(node); $('nodes').append(card);\r\n            state.layout.push({ id: node.id, card, x: center ? 600 : 600 + Math.cos(angle) * radiusX,\r\n                y: center ? 370 : 370 + Math.sin(angle) * radiusY, width: center ? 232 : 186, height: center ? 122 : 102, phase: i * 1.73 });\r\n        }\r\n        if (hasCenter) for (const node of view.nodes) view.edges.push({ source: null, target: node.id, kind: 'contains', count: 1, structural: true });\r\n        // Bound DOM work even for a very dense neighborhood; details retain all direct edges.\r\n        const edgeLimit = compact ? 2500 : 180;\r\n        const shownEdges = view.edges.slice(0, edgeLimit);\r\n        const pairCounts = new Map();\r\n        for (const edge of shownEdges) {\r\n            const group = svg('g', { class: 'edge-group' });\r\n            group.dataset.source = edge.source ?? ''; group.dataset.target = edge.target;\r\n            const path = svg('path', { class: 'edge' });\r\n            if (!compact) path.setAttribute('marker-end', 'url(#arrow)');\r\n            if (edge.structural && !compact) { path.style.strokeDasharray = '3 7'; path.style.strokeOpacity = '.17'; }\r\n            const sourceNode = state.index.nodes.get(edge.source);\r\n            if (compact) {\r\n                const color = sourceNode ? category(sourceNode).color : 'var(--node-module)';\r\n                group.style.setProperty('--edge-color', color);\r\n                const cross = positions.get(edge.source)?.group !== positions.get(edge.target)?.group;\r\n                group.classList.toggle('cross-link', cross);\r\n            }\r\n            const flow = svg('path', { class: 'edge-flow', ...(compact ? { pathLength: '1' } : {}) });\r\n            flow.style.animationDelay = `${-(state.edges.length % 13) * .19}s`;\r\n            const hit = svg('path', { class: 'edge-hit' });\r\n            const label = svg('text', { class: 'edge-label', 'text-anchor': 'middle' });\r\n            label.textContent = edge.kind + (edge.count > 1 ? ` ×${edge.count}` : '');\r\n            const title = svg('title');\r\n            const sourceName = state.index.nodes.get(edge.source)?.name ?? state.graph.repository.name;\r\n            title.textContent = `${sourceName} → ${state.index.nodes.get(edge.target).name}: ${label.textContent}`;\r\n            group.append(title, path);\r\n            if (!edge.structural) group.append(flow);\r\n            group.append(hit, label); $('edge-layer').append(group);\r\n            const key = JSON.stringify([edge.source, edge.target].sort());\r\n            const parallel = pairCounts.get(key) ?? 0; pairCounts.set(key, parallel + 1);\r\n            state.edges.push({ ...edge, group, path, flow, hit, label, compact, bend: (compact ? 8 : 30) + parallel * 24 });\r\n        }\r\n        if (compact) state.pivot = S.center(state.layout);\r\n        state.viewSummary = { nodes: view.nodes.length, total: view.total, compact, edges: shownEdges.length, edgeTotal: view.edges.length, edgeLimit };\r\n        applySearchVisibility();\r\n        applyChangeHighlight();\r\n        fitGraph();\r\n        revealGraph();\r\n        draw(0); schedule();\r\n    }\r\n    function setTheme(theme = {}) {\r\n        const resolved = Theme.resolve(theme); // Validate completely before changing any styles.\r\n        $('atlas-theme').textContent = Theme.toCss(resolved);\r\n        document.documentElement.dataset.atlasTheme = resolved.mode;\r\n        document.querySelector('meta[name=\"color-scheme\"]').content = resolved.mode;\r\n        currentTheme = resolved;\r\n        return getTheme();\r\n    }\r\n    function getTheme() { return structuredClone(currentTheme); }\r\n    function getChangeSummary() {\r\n        return structuredClone({ ...state.changes, enabled: state.highlightChanges });\r\n    }\r\n    function setChangedFiles(files) {\r\n        const changes = Changes.match(state.index, files); // Validate before replacing the overlay.\r\n        state.changes = changes; state.changedIds = new Set(changes.nodeIds);\r\n        applyChangeHighlight();\r\n        return getChangeSummary();\r\n    }\r\n    function setHighlightChanges(enabled) {\r\n        if (typeof enabled !== 'boolean') throw new TypeError('highlightChanges must be a boolean.');\r\n        state.highlightChanges = enabled; applyChangeHighlight();\r\n        return getChangeSummary();\r\n    }\r\n    function updateChangeSummary() {\r\n        const changes = state.changes;\r\n        if (!changes) return;\r\n        const toggle = $('highlight-changes'), summary = $('change-summary');\r\n        toggle.hidden = summary.hidden = !changes.changedFiles.length;\r\n        toggle.setAttribute('aria-pressed', String(state.highlightChanges));\r\n        const visible = state.layout.filter(item => state.changedIds.has(item.id) && !item.card.hidden).length;\r\n        summary.textContent = `${state.highlightChanges ? 'Changed files' : 'Highlighting off'} · ${changes.matchedFiles.length}/${changes.changedFiles.length} files matched · ${changes.nodeIds.length} entities (${visible} in view)`\r\n            + (changes.unmatchedFiles.length ? ` · ${changes.unmatchedFiles.length} ${changes.unmatchedFiles.length === 1 ? 'file' : 'files'} not in snapshot` : '');\r\n        summary.title = summary.textContent + '\\nEntities in supplied changed files; this does not identify changed lines or dependency impact.'\r\n            + (changes.unmatchedFiles.length ? '\\nUnmatched: ' + changes.unmatchedFiles.slice(0, 20).join(', ') : '');\r\n    }\r\n    function applyChangeHighlight() {\r\n        for (const item of state.layout) {\r\n            const matched = state.changedIds.has(item.id), active = state.highlightChanges && matched;\r\n            item.card.classList.toggle('change-match', active);\r\n            const node = state.index.nodes.get(item.id);\r\n            if (node) item.card.setAttribute('aria-label', `${node.kind}: ${node.name}${active ? ' · In a changed file' : ''}`);\r\n        }\r\n        highlight(state.hovered ?? state.selected);\r\n        updateChangeSummary();\r\n    }\r\n    $('highlight-changes').addEventListener('click', () => setHighlightChanges(!state.highlightChanges));\r\n    function highlight(id) {\r\n        const connected = new Set([id]);\r\n        if (id !== null) for (const edge of state.edges) if (edge.source === id || edge.target === id) { connected.add(edge.source); connected.add(edge.target); }\r\n        const changeFocus = state.highlightChanges && state.changedIds.size > 0;\r\n        for (const item of state.layout) {\r\n            // Keep changed entities bright. A hovered/selected unchanged node remains inspectable.\r\n            const dim = changeFocus ? !state.changedIds.has(item.id) && (id === null || item.id !== id)\r\n                : id !== null && !connected.has(item.id);\r\n            item.card.classList.toggle('dim', dim);\r\n        }\r\n        for (const edge of state.edges) {\r\n            const active = id !== null && (edge.source === id || edge.target === id);\r\n            edge.group.classList.toggle('highlight', active);\r\n            edge.group.classList.toggle('dim', id !== null && !active);\r\n            edge.group.classList.toggle('change-muted', changeFocus && !active\r\n                && !(state.changedIds.has(edge.source) && state.changedIds.has(edge.target)));\r\n        }\r\n    }\r\n    function pointAtBoundary(from, to) {\r\n        const dx = to.x - from.x, dy = to.y - from.y;\r\n        const scale = 1 / Math.max(Math.abs(dx) / (from.width / 2 + 5), Math.abs(dy) / (from.height / 2 + 5), .001);\r\n        return { x: from.x + dx * scale, y: from.y + dy * scale };\r\n    }\r\n    function draw(timestamp) {\r\n        const elapsed = timestamp && state.last ? Math.min(timestamp - state.last, 60) : 0;\r\n        state.time += elapsed;\r\n        state.last = timestamp;\r\n        advanceMotion(elapsed);\r\n        const moving = !motionQuery.matches;\r\n        if (moving) {\r\n            state.pointer.x += (state.pointerTarget.x - state.pointer.x) * .045;\r\n            state.pointer.y += (state.pointerTarget.y - state.pointer.y) * .045;\r\n        }\r\n        const positions = new Map();\r\n        for (const item of state.layout) {\r\n            const offset = moving ? Math.sin(state.time / 2300 + item.phase) * 6 : 0;\r\n            // Freeze ambient drift in world space; selected and pinned nodes still turn with the graph.\r\n            const held = state.hovered === item.id || state.selected === item.id;\r\n            const world = item.compact ? pinFor(item.id) ?? (held && item.lastWorld ? item.lastWorld : worldPosition(item)) : null;\r\n            const stable = item.compact ? S.projectPoint(world, state.orbit, state.pivot)\r\n                : held && item.lastPosition ? item.lastPosition : { x: item.x, y: item.y + offset, brightness: 1 };\r\n            const position = { ...item, ...stable };\r\n            item.lastWorld = world;\r\n            item.lastPosition = stable;\r\n            positions.set(item.id, position);\r\n            item.card.style.transform = `translate(${position.x - item.width / 2}px,${position.y - item.height / 2}px)`;\r\n            if (item.compact) {\r\n                item.card.style.setProperty('--node-opacity', stable.brightness.toFixed(3));\r\n                item.card.style.setProperty('--depth-scale', stable.scale.toFixed(3));\r\n            }\r\n        }\r\n        if (constellation()) {\r\n            const sorted = [...state.layout].sort((a, b) => a.lastPosition.depth - b.lastPosition.depth);\r\n            sorted.forEach((item, index) => { item.card.style.zIndex = index + 1; });\r\n            $('stage').dataset.yaw = state.orbit.yaw.toFixed(4);\r\n            $('stage').dataset.pitch = state.orbit.pitch.toFixed(4);\r\n            placeLabels();\r\n        }\r\n        for (const edge of state.edges) {\r\n            const from = positions.get(edge.source), to = positions.get(edge.target);\r\n            if (!from || !to) continue;\r\n            let path, labelX, labelY;\r\n            if (edge.source === edge.target) {\r\n                const radius = edge.compact ? 16 : 65;\r\n                path = `M${from.x - radius * .7},${from.y - radius * .7} C${from.x - radius * 2},${from.y - radius * 2.3} ${from.x + radius * 2},${from.y - radius * 2.3} ${from.x + radius * .7},${from.y - radius * .7}`;\r\n                labelX = from.x; labelY = from.y - radius * 2;\r\n            } else {\r\n                const start = edge.compact ? from : pointAtBoundary(from, to), end = edge.compact ? to : pointAtBoundary(to, from);\r\n                const dx = end.x - start.x, dy = end.y - start.y, length = Math.hypot(dx, dy) || 1;\r\n                const cx = (start.x + end.x) / 2 - dy / length * edge.bend;\r\n                const cy = (start.y + end.y) / 2 + dx / length * edge.bend;\r\n                path = `M${start.x},${start.y} Q${cx},${cy} ${end.x},${end.y}`;\r\n                labelX = (start.x + end.x + 2 * cx) / 4;\r\n                labelY = (start.y + end.y + 2 * cy) / 4 - 7;\r\n            }\r\n            for (const node of [edge.path, edge.flow, edge.hit]) node.setAttribute('d', path);\r\n            edge.label.setAttribute('x', labelX); edge.label.setAttribute('y', labelY);\r\n        }\r\n    }\r\n    function worldPosition(item) {\r\n        return S.position(item, motionQuery.matches ? 0 : state.time, motionQuery.matches ? { x: 0, y: 0 } : state.pointer);\r\n    }\r\n    function projectItem(item) {\r\n        return S.projectPoint(pinFor(item.id) ?? worldPosition(item), state.orbit, state.pivot);\r\n    }\r\n    function wrapAngle(angle) { return Math.atan2(Math.sin(angle), Math.cos(angle)); }\r\n    function motionEnabled() { return !state.starting && !motionQuery.matches && !document.hidden; }\r\n    function cameraState() { return { zoom: state.zoom, panX: state.panX, panY: state.panY }; }\r\n    function reveal(element, frames, duration = 500, delay = 0) {\r\n        if (!motionEnabled()) return;\r\n        for (const animation of state.animations) if (animation.effect?.target === element) {\r\n            animation.cancel(); state.animations.delete(animation);\r\n        }\r\n        const animation = element.animate(frames, { duration, delay, easing: 'cubic-bezier(.16,1,.3,1)', fill: 'backwards' });\r\n        state.animations.add(animation);\r\n        const clean = () => state.animations.delete(animation);\r\n        animation.addEventListener('finish', clean, { once: true });\r\n        animation.addEventListener('cancel', clean, { once: true });\r\n    }\r\n    function finishReveals() {\r\n        for (const animation of state.animations) animation.cancel();\r\n        state.animations.clear();\r\n    }\r\n    function stopChoreography() {\r\n        state.transition = null; state.inertia = null;\r\n        $('stage').dataset.motion = 'idle';\r\n    }\r\n    function transitionTo(orbit, camera, duration, kind) {\r\n        stopChoreography();\r\n        if (!motionEnabled()) {\r\n            state.orbit = { ...orbit }; Object.assign(state, camera); applyCamera(); draw(0); return;\r\n        }\r\n        state.transition = { fromOrbit: { ...state.orbit }, orbit: { ...orbit }, fromCamera: cameraState(),\r\n            camera: { ...camera }, duration, elapsed: 0, kind };\r\n        $('stage').dataset.motion = kind;\r\n        schedule();\r\n    }\r\n    function advanceMotion(elapsed) {\r\n        if (!motionEnabled() || drag || nodeDrag) return;\r\n        const transition = state.transition;\r\n        if (transition) {\r\n            transition.elapsed += elapsed;\r\n            const t = Math.min(1, transition.elapsed / transition.duration);\r\n            const eased = t * t * t * (t * (t * 6 - 15) + 10);\r\n            for (const axis of ['yaw', 'pitch']) state.orbit[axis] = wrapAngle(transition.fromOrbit[axis]\r\n                + wrapAngle(transition.orbit[axis] - transition.fromOrbit[axis]) * eased);\r\n            for (const key of ['zoom', 'panX', 'panY']) state[key] = transition.fromCamera[key]\r\n                + (transition.camera[key] - transition.fromCamera[key]) * eased;\r\n            if (t === 1) { state.orbit = { ...transition.orbit }; state.transition = null; }\r\n            applyCamera(false);\r\n        } else if (constellation()) {\r\n            if (state.inertia) {\r\n                const decay = Math.exp(-elapsed / 150), distance = 150 * (1 - decay);\r\n                for (const axis of ['yaw', 'pitch']) {\r\n                    state.orbit[axis] = wrapAngle(state.orbit[axis] + state.inertia[axis] * distance);\r\n                    state.inertia[axis] *= decay;\r\n                }\r\n                if (Math.hypot(state.inertia.yaw, state.inertia.pitch) \u003c .00002) state.inertia = null;\r\n            }\r\n        }\r\n        $('stage').dataset.motion = state.transition?.kind ?? (state.inertia ? 'coasting' : 'idle');\r\n    }\r\n    function revealGraph() {\r\n        if (state.restoring || !motionEnabled() || !state.layout.length) return;\r\n        const compact = constellation();\r\n        for (const [index, item] of state.layout.entries()) {\r\n            const delay = compact ? (item.phase / (Math.PI * 2) % 1) * 260 + L.hash(String(item.id)) * 95 : Math.min(index * 24, 280);\r\n            reveal(item.card, [{ opacity: 0 }, {}], 560, delay); // Fade to current CSS opacity, including change dimming.\r\n            if (compact) reveal(item.card.querySelector('.orb-shape'), [{ transform: 'scale(.5)' }, { transform: 'scale(1)' }], 680, delay);\r\n        }\r\n        reveal($('edge-layer'), [{ opacity: 0 }, { opacity: 1 }], 750, 160);\r\n        if (compact && !state.selected) {\r\n            const target = { ...state.orbit }, camera = cameraState();\r\n            state.orbit = { yaw: target.yaw - .09, pitch: target.pitch + .04 };\r\n            transitionTo(target, camera, 850, 'entrance');\r\n        }\r\n    }\r\n    function returnToPanAfterIdle() {\r\n        clearTimeout(rotateTimer);\r\n        if (state.interaction !== 'rotate' || drag || nodeDrag || rotationKeys.size) return;\r\n        rotateTimer = setTimeout(() => {\r\n            state.interaction = 'pan'; updateGestures();\r\n        }, 3000);\r\n    }\r\n    function setInteraction(mode) {\r\n        clearTimeout(rotateTimer); rotationKeys.clear();\r\n        stopChoreography();\r\n        state.interaction = constellation() ? mode : 'pan';\r\n        updateGestures(); returnToPanAfterIdle();\r\n    }\r\n    function updateGestures() {\r\n        const rotating = constellation() && state.interaction === 'rotate';\r\n        $('stage').classList.toggle('rotate-mode', rotating);\r\n        $('rotate-mode').setAttribute('aria-pressed', String(rotating));\r\n        $('pan-mode').setAttribute('aria-pressed', String(!rotating));\r\n        $('rotate-mode').disabled = !constellation();\r\n        $('stage').setAttribute('aria-label', rotating\r\n            ? 'Code graph. Drag canvas or use arrow keys to rotate. Shift-drag or Shift-arrow to pan. Scroll to zoom. Drag a node to move it.'\r\n            : 'Code graph. Drag canvas or use arrow keys to pan. Scroll to zoom. Select Rotate to turn the graph. Drag a node to move it.');\r\n    }\r\n    function resetView() {\r\n        finishReveals();\r\n        const orbit = { yaw: 0, pitch: 0 };\r\n        transitionTo(orbit, fittedCamera(orbit) ?? cameraState(), 950, 'reset');\r\n    }\r\n    function tick(timestamp) {\r\n        if (!motionEnabled()) return;\r\n        if (!state.last || timestamp - state.last >= 30) draw(timestamp);\r\n        state.frame = requestAnimationFrame(tick);\r\n    }\r\n    function schedule() {\r\n        cancelAnimationFrame(state.frame); state.last = 0;\r\n        if (motionEnabled()) state.frame = requestAnimationFrame(tick);\r\n    }\r\n    function applyCamera(labels = true) {\r\n        $('world').style.transform = `translate(${state.panX}px,${state.panY}px) scale(${state.zoom})`;\r\n        $('world').style.setProperty('--glyph-scale', Math.min(2.4, Math.max(.8, 1 / state.zoom)).toFixed(3));\r\n        $('world').style.setProperty('--label-size', `${Math.min(29, Math.max(10, 12 / state.zoom)).toFixed(2)}px`);\r\n        $('world').style.setProperty('--label-line', `${Math.min(38, Math.max(13, 17 / state.zoom)).toFixed(2)}px`);\r\n        $('zoom-value').textContent = `${Math.round(state.zoom * 100)}%`;\r\n        $('stage').classList.toggle('zoomed', state.zoom > 1.55);\r\n        $('node-tooltip').hidden = true;\r\n        if (labels && constellation()) placeLabels();\r\n    }\r\n    function animateCamera(camera) {\r\n        transitionTo(state.orbit, camera, 650, 'focus');\r\n    }\r\n    function dismissSelection() {\r\n        state.selected = null; state.hovered = null;\r\n        renderInspector(); renderBreadcrumbs(); highlight(null);\r\n        for (const item of state.layout) { item.card.classList.remove('selected'); item.card.setAttribute('aria-pressed', 'false'); }\r\n        $('view-hint').hidden = false; $('node-tooltip').hidden = true;\r\n    }\r\n    function clearSelection(record = true) {\r\n        if (record && state.selected !== null) rememberView();\r\n        stopChoreography();\r\n        if (!constellation() || state.scope !== state.overviewScope) { goScope(constellation() ? state.overviewScope : state.scope, false); return; }\r\n        dismissSelection();\r\n        if (state.overviewCamera) animateCamera(state.overviewCamera);\r\n        state.overviewCamera = null;\r\n    }\r\n    function placeLabels() {\r\n        // Read the viewport once before class changes; per-node reads force repeated layouts.\r\n        const widthAvailable = $('stage').clientWidth, heightAvailable = $('stage').clientHeight;\r\n        const occupied = [], candidates = [...state.layout].sort((a, b) => {\r\n            const score = item => (L.family(item.node) === 'module' ? 10000 : 0) + state.index.adjacency.get(item.id).length;\r\n            return score(b) - score(a);\r\n        });\r\n        let count = 0;\r\n        for (const item of candidates) {\r\n            item.card.classList.remove('named');\r\n            if (item.card.hidden) continue;\r\n            const family = L.family(item.node);\r\n            if (family !== 'module' && count >= (state.query || state.filter === 'classes' ? 23 : 17)) continue;\r\n            if (!state.query && family === 'function' && state.index.adjacency.get(item.id).length \u003c 3) continue;\r\n            const point = item.lastPosition ?? projectItem(item);\r\n            const x = point.x * state.zoom + state.panX + 18, y = point.y * state.zoom + state.panY - 7;\r\n            const width = Math.min(150, item.node.name.length * 6.8 + 12), height = 21;\r\n            if (family !== 'module' && occupied.some(box => x \u003c box.x + box.width && x + width > box.x && y \u003c box.y + box.height && y + height > box.y)) continue;\r\n            if (x \u003c 10 || x + width > widthAvailable - 65 || y \u003c 90 || y > heightAvailable - 65) continue;\r\n            occupied.push({ x, y, width, height }); item.card.classList.add('named'); count++;\r\n        }\r\n    }\r\n    function fittedCamera(orbit = state.orbit) {\r\n        const width = $('stage').clientWidth, height = $('stage').clientHeight;\r\n        const items = state.layout.filter(item => !item.card.hidden).map(item => item.compact\r\n            ? { ...item, ...S.projectPoint(pinFor(item.id) ?? worldPosition(item), orbit, state.pivot) } : item);\r\n        if (!items.length) return;\r\n        // Fit the current projection with a small screen-space margin, not old UI gutters.\r\n        const minX = Math.min(...items.map(n => n.x - n.width / 2)) - 8;\r\n        const maxX = Math.max(...items.map(n => n.x + n.width / 2)) + 8;\r\n        const minY = Math.min(...items.map(n => n.y - n.height / 2)) - 8;\r\n        const maxY = Math.max(...items.map(n => n.y + n.height / 2)) + 8;\r\n        const padding = 20;\r\n        const zoom = Math.max(.12, Math.min(2, Math.max(1, width - padding * 2) / Math.max(1, maxX - minX),\r\n            Math.max(1, height - padding * 2) / Math.max(1, maxY - minY)));\r\n        return { zoom, panX: width / 2 - (minX + maxX) / 2 * zoom, panY: height / 2 - (minY + maxY) / 2 * zoom };\r\n    }\r\n    function fitGraph() {\r\n        const camera = fittedCamera();\r\n        if (!camera) return;\r\n        Object.assign(state, camera);\r\n        applyCamera();\r\n    }\r\n    function zoom(factor, x = $('stage').clientWidth / 2, y = $('stage').clientHeight / 2) {\r\n        stopChoreography(); finishReveals();\r\n        const next = Math.min(2.4, Math.max(.12, state.zoom * factor)), ratio = next / state.zoom;\r\n        state.panX = x - (x - state.panX) * ratio; state.panY = y - (y - state.panY) * ratio;\r\n        state.zoom = next; applyCamera();\r\n    }\r\n    function metricGrid(node) {\r\n        const grid = el('div', 'metric-grid');\r\n        const values = [['Lines of code', count(node.metrics?.loc)], ['Complexity', count(node.metrics?.complexity)],\r\n            ['Coverage', node.metrics?.coverage === undefined ? '—' : `${count(node.metrics.coverage)}%`], ['Direct links', count(state.index.adjacency.get(node.id).length)]];\r\n        for (const [label, value] of values) {\r\n            const cell = el('div', 'metric'); cell.append(el('strong', '', value), el('span', '', label)); grid.append(cell);\r\n        }\r\n        return grid;\r\n    }\r\n    function relationshipRow(item, closeDialog = false) {\r\n        const row = button('', 'relationship', () => {\r\n            if (closeDialog) {\r\n                rememberView(); state.detailId = null; previousFocus = null; $('details-dialog').close();\r\n            }\r\n            focusNode(item.node.id, !closeDialog);\r\n        });\r\n        const text = el('span', '', item.node.name); text.append(el('small', '', item.label));\r\n        row.append(el('span', '', item.direction ?? '◇'), text, el('span', '', '↗'));\r\n        return row;\r\n    }\r\n    function renderInspector() {\r\n        const inspector = $('inspector'); inspector.hidden = !state.selected;\n        if (inspector.dataset.inspectedNode !== (state.selected || '')) {\n            inspector.scrollTop = 0;\n            inspector.dataset.inspectedNode = state.selected || '';\n        }\r\n        if (!state.selected) { inspector.replaceChildren(); return; }\r\n        const node = state.index.nodes.get(state.selected);\r\n        const header = el('div', 'inspector-header', 'ENTITY INSPECTOR');\r\n        const close = button('×', 'icon-button', clearSelection); close.setAttribute('aria-label', 'Close inspector'); header.append(close);\r\n        const kind = el('div', 'entity-kind', `${node.kind} / ${node.language ?? 'Group'}`); kind.style.color = category(node).color;\r\n        inspector.replaceChildren(header, kind, el('h2', '', node.name), el('div', 'entity-path', node.path ?? 'No source path supplied'), el('p', 'entity-summary', node.summary ?? 'No description supplied for this entity.'), metricGrid(node));\r\n        inspector.append(button('Open details  ↗', 'button primary', () => openDetails(node.id)));\r\n        if (childNodes(node.id).length) inspector.append(button(`Explore ${childNodes(node.id).length} contained entities  →`, 'button secondary', () => goScope(node.id)));\r\n        const connections = related(node.id);\r\n        inspector.append(el('div', 'section-label', `Connections / ${connections.length}`));\r\n        for (const item of connections.slice(0, 6)) inspector.append(relationshipRow(item));\r\n        if (!connections.length) inspector.append(el('p', 'quiet', 'No direct relationships supplied. Explore the contents to see internal connections.'));\r\n        if (connections.length > 6) inspector.append(button(`View all ${connections.length} connections →`, 'button secondary', () => openDetails(node.id, 'connections')));\r\n        inspector.append(el('p', 'quiet', '— means this metric was not supplied.'));\r\n        reveal(inspector, [{ opacity: 0, translate: '16px 0' }, { opacity: 1, translate: '0 0' }], 430);\r\n        inspector.querySelectorAll('.metric').forEach((cell, index) => reveal(cell,\r\n            [{ opacity: 0, translate: '0 6px' }, { opacity: 1, translate: '0 0' }], 400, 80 + index * 40));\r\n    }\r\n    function openDetails(id, tab = 'overview') {\r\n        if (onOpenDetails) {\r\n            stopChoreography(); finishReveals();\r\n            try {\r\n                Promise.resolve(onOpenDetails({ ...entityDetails(id), tab })).catch(error => toast(`Could not open details: ${error.message ?? error}`, true));\r\n            } catch (error) { toast(`Could not open details: ${error.message ?? error}`, true); }\r\n            return;\r\n        }\r\n        if (state.detailId !== id) rememberView();\r\n        stopChoreography(); finishReveals();\r\n        state.dialogTab = tab; state.detailId = id;\r\n        previousFocus = document.activeElement;\r\n        renderDetails(id);\r\n        if (!$('details-dialog').open) $('details-dialog').showModal();\r\n    }\r\n    function renderDetails(id) {\r\n        const node = state.index.nodes.get(id), content = $('dialog-content');\r\n        const top = el('div', 'dialog-top'), heading = el('div');\r\n        heading.append(el('div', 'entity-kind', `${node.kind} / ${node.language ?? 'Group'}${state.graph.repository.sample ? ' / FICTIONAL SAMPLE' : ''}`));\r\n        const title = el('h2', '', node.name); title.id = 'detail-title';\r\n        heading.append(title, el('div', 'entity-path', node.path ?? 'No source path supplied'));\r\n        const back = button('← Back', 'back-button', navigateBack); back.id = 'dialog-back'; back.setAttribute('aria-label', 'Back to previous screen');\r\n        const close = button('×', 'icon-button', navigateBack); close.setAttribute('aria-label', 'Close details');\r\n        top.append(heading, close);\r\n        const tabs = el('div', 'detail-tabs'); tabs.setAttribute('role', 'tablist'); tabs.setAttribute('aria-label', 'Entity details');\r\n        for (const key of ['overview', 'connections', 'history']) {\r\n            const tab = button(key[0].toUpperCase() + key.slice(1), 'detail-tab', () => {\r\n                state.dialogTab = key; renderDetails(id); document.getElementById(`tab-${key}`).focus();\r\n            });\r\n            tab.id = `tab-${key}`; tab.setAttribute('role', 'tab'); tab.setAttribute('aria-selected', String(state.dialogTab === key));\r\n            tab.setAttribute('aria-controls', 'detail-panel'); tab.tabIndex = state.dialogTab === key ? 0 : -1;\r\n            tab.addEventListener('keydown', event => {\r\n                if (!['ArrowLeft', 'ArrowRight', 'Home', 'End'].includes(event.key)) return;\r\n                event.preventDefault();\r\n                const keys = ['overview', 'connections', 'history'], index = keys.indexOf(key);\r\n                const target = event.key === 'Home' ? 0 : event.key === 'End' ? 2 : (index + (event.key === 'ArrowRight' ? 1 : 2)) % 3;\r\n                state.dialogTab = keys[target]; renderDetails(id); $(`tab-${keys[target]}`).focus();\r\n            });\r\n            tabs.append(tab);\r\n        }\r\n        const panel = el('div', 'detail-panel'); panel.id = 'detail-panel'; panel.setAttribute('role', 'tabpanel');\r\n        panel.setAttribute('aria-labelledby', `tab-${state.dialogTab}`); panel.tabIndex = 0;\r\n        if (state.dialogTab === 'overview') {\r\n            panel.append(el('p', 'entity-summary', node.summary ?? 'No description supplied.'), metricGrid(node));\r\n            if (node.signature) panel.append(el('pre', 'signature', node.signature));\r\n            const children = childNodes(node.id);\r\n            if (children.length) {\r\n                panel.append(el('div', 'section-label', `Contains / ${children.length}`));\r\n                const list = el('div', 'detail-list');\r\n                for (const child of children) list.append(relationshipRow({ node: child, label: child.kind, direction: '◇' }, true));\r\n                panel.append(list);\r\n            }\r\n            const known = new Set(['loc', 'complexity', 'coverage']);\r\n            for (const [key, value] of Object.entries(node.metrics ?? {})) if (!known.has(key)) panel.append(el('p', 'quiet', `${key}: ${count(value)}`));\r\n            if (node.links?.length) {\r\n                const links = el('div', 'detail-links');\r\n                for (const link of node.links) {\r\n                    const a = el('a', '', `${link.label} ↗`); a.href = C.safeUrl(link.url); a.target = '_blank'; a.rel = 'noopener noreferrer'; links.append(a);\r\n                }\r\n                panel.append(links);\r\n            }\r\n        } else if (state.dialogTab === 'connections') {\r\n            const items = related(id);\r\n            panel.append(el('p', 'quiet', `${items.length} direct relationships. Select an entity to follow the connection.`));\r\n            const list = el('div', 'detail-list');\r\n            for (const item of items) list.append(relationshipRow(item, true));\r\n            panel.append(list);\r\n            if (!items.length) panel.append(el('p', 'quiet', 'No direct relationships supplied. Containment is available on the Overview tab.'));\r\n        } else {\r\n            const strip = el('div', 'changes-strip');\r\n            strip.append(el('span', 'additions', `+${count(node.changes?.added)} lines`), el('span', 'deletions', `−${count(node.changes?.removed)} lines`), el('span', '', `${count(node.changes?.commits)} commits`));\r\n            panel.append(strip);\r\n            if (!node.history?.length) panel.append(el('p', 'quiet', 'No Git history supplied for this entity. A producer can attach commit records to any node.'));\r\n            for (const entry of node.history ?? []) {\r\n                const row = el('article', 'history-entry'); row.append(el('h3', '', entry.message));\r\n                const meta = el('p');\r\n                if (entry.url) {\r\n                    const a = el('a', '', entry.commit.slice(0, 8)); a.href = C.safeUrl(entry.url); a.target = '_blank'; a.rel = 'noopener noreferrer'; a.style.marginRight = '13px'; meta.append(a);\r\n                } else meta.append(el('code', '', entry.commit.slice(0, 8)));\r\n                meta.append(document.createTextNode(`${entry.author} · ${new Date(entry.date).toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: 'numeric', timeZone: 'UTC' })}`));\r\n                row.append(meta); panel.append(row);\r\n            }\r\n            panel.append(el('p', 'quiet', state.graph.repository.sample ? 'These commits and change totals are fictional sample data.' : 'History and change totals reflect the producer’s snapshot and reporting window.'));\r\n        }\r\n        content.replaceChildren(back, top, tabs, panel); updateBack();\r\n        reveal(panel, [{ opacity: 0, translate: '0 8px' }, { opacity: 1, translate: '0 0' }], 320);\r\n    }\r\n    function applySearchVisibility() {\r\n        if (!state.viewSummary) return;\r\n        const matching = new Set();\r\n        for (const item of state.layout) {\r\n            const wasHidden = item.card.hidden;\r\n            item.card.hidden = !!state.query && !state.matches.has(item.id);\r\n            if (wasHidden && !item.card.hidden) reveal(item.card, [{ opacity: 0 }, {}], 260);\r\n            if (!item.card.hidden && item.id !== null) matching.add(item.id);\r\n        }\r\n        for (const edge of state.edges) {\r\n            const hidden = !!state.query && (!matching.has(edge.source) || !matching.has(edge.target));\r\n            edge.group.toggleAttribute('hidden', hidden);\r\n        }\r\n        const view = state.viewSummary;\r\n        $('empty-map').hidden = matching.size > 0;\r\n        $('empty-map').textContent = state.query\r\n            ? state.matches.size ? 'No matches in this view. Choose a search result or change the scope.' : `No entities match “${$('search').value.trim()}”.`\r\n            : 'No entities in this view.';\r\n        const notices = [];\r\n        if (view.total > view.nodes) notices.push(`Showing ${view.nodes} of ${view.total} entities; use search for the rest`);\r\n        if (view.edgeTotal > view.edgeLimit) notices.push('Links limited; all direct links in details');\r\n        $('view-summary').textContent = notices.join(' · ');\r\n        $('view-summary').hidden = !notices.length;\r\n        for (const kind of ['module', 'class', 'file', 'function', 'data']) {\r\n            $('count-' + kind).textContent = state.graph.nodes.filter(n => L.family(n) === kind && (!state.query || matching.has(n.id))).length;\r\n        }\r\n        document.querySelector('.graph-legend').setAttribute('aria-label', state.query ? 'Matching node types and counts' : 'Node types and counts');\r\n        updateChangeSummary();\r\n        if (constellation()) placeLabels();\r\n    }\r\n    function clearSearch(record = true) {\r\n        $('search').value = ''; search(record);\r\n    }\r\n    function setSearchValue(value) {\r\n        state.searchText = value; $('search').value = value;\r\n        state.query = value.trim().toLowerCase();\r\n        const matches = state.graph.nodes.filter(n => [n.name, n.path, n.kind, n.language].some(field => field?.toLowerCase().includes(state.query)));\r\n        state.matches = state.query ? new Set(matches.map(node => node.id)) : null;\r\n        $('clear-search').hidden = !value;\r\n        $('search').closest('.search-wrap').classList.toggle('has-query', !!value);\r\n        $('search-results').hidden = true;\r\n        return matches;\r\n    }\r\n    function search(record = true) {\r\n        if (record && state.selected !== null) rememberView();\r\n        stopChoreography(); finishReveals();\r\n        const matches = setSearchValue($('search').value), query = state.query, results = $('search-results');\r\n        // A new query dismisses the old selection without moving the camera or re-laying out nodes.\r\n        if (state.selected) { dismissSelection(); state.overviewCamera = null; }\r\n        state.hovered = null; $('node-tooltip').hidden = true; highlight(null);\r\n        $('clear-search').hidden = !$('search').value;\r\n        $('search').closest('.search-wrap').classList.toggle('has-query', !!$('search').value);\r\n        applySearchVisibility();\r\n        results.replaceChildren();\r\n        if (!query) { results.hidden = true; return; }\r\n        results.append(el('div', 'search-summary', `${matches.length} ${matches.length === 1 ? 'match' : 'matches'} across repository`));\r\n        for (const node of matches.slice(0, 80)) {\r\n            const item = button(node.name, 'search-result', () => { focusNode(node.id); $('inspector').querySelector('button').focus(); });\r\n            item.append(el('small', '', `${node.kind} · ${node.path ?? node.language ?? 'Group'}`)); results.append(item);\r\n        }\r\n        if (!matches.length) results.append(el('div', 'search-empty', `No entities match “${$('search').value}”.`));\r\n        if (matches.length > 80) results.append(el('div', 'search-empty', `Showing 80 of ${matches.length}. Refine your search.`));\r\n        results.hidden = false;\r\n    }\r\n    function changeDisplay(display) {\r\n        if (state.display === display) return;\r\n        rememberView(); state.display = display; goScope(state.selected ? state.overviewScope : state.scope, false);\r\n    }\r\n    $('constellation-button').addEventListener('click', () => changeDisplay('constellation'));\r\n    $('cards-button').addEventListener('click', () => changeDisplay('cards'));\r\n    $('entity-filter').addEventListener('change', () => { rememberView(); state.filter = $('entity-filter').value; goScope(state.scope, false); });\r\n    $('back-button').addEventListener('click', navigateBack);\r\n    $('zoom-in').addEventListener('click', () => zoom(1.2));\r\n    $('zoom-out').addEventListener('click', () => zoom(1 / 1.2));\r\n    $('fit-button').addEventListener('click', () => {\r\n        finishReveals();\r\n        transitionTo(state.orbit, fittedCamera() ?? cameraState(), 650, 'fit');\r\n    });\r\n    $('reset-layout').addEventListener('click', () => {\r\n        state.pinned.clear(); clearSelection(false);\r\n        for (const item of state.layout) item.lastWorld = null;\r\n        stopChoreography(); draw(0); fitGraph(); toast('Moved nodes reset.');\r\n    });\r\n    $('rotate-mode').addEventListener('click', () => setInteraction('rotate'));\r\n    $('pan-mode').addEventListener('click', () => setInteraction('pan'));\r\n    $('reset-view').addEventListener('click', resetView);\r\n    $('stage').addEventListener('wheel', event => {\r\n        event.preventDefault(); const bounds = $('stage').getBoundingClientRect();\r\n        zoom(Math.exp(-event.deltaY * .001), event.clientX - bounds.left, event.clientY - bounds.top);\r\n    }, { passive: false });\r\n    $('stage').addEventListener('pointerdown', event => {\r\n        if (drag || nodeDrag) return;\r\n        const node = event.target.closest('.node.orb');\r\n        if (event.button === 0 && node && !event.shiftKey) {\r\n            const item = state.layout.find(item => item.id === node.dataset.nodeId);\r\n            if (!item) return;\r\n            stopChoreography(); finishReveals();\r\n            clearTimeout(rotateTimer);\r\n            const point = item.lastPosition ?? projectItem(item);\r\n            nodeDrag = { id: item.id, pointerId: event.pointerId, x: event.clientX, y: event.clientY, startX: point.x, startY: point.y, depth: point.depth };\r\n            node.setPointerCapture(event.pointerId); return;\r\n        }\r\n        if (![0, 1].includes(event.button) || (!node && event.target.closest('button,select,input'))) return;\r\n        if (node && event.button === 0 && !event.shiftKey) return;\r\n        event.preventDefault();\r\n        stopChoreography(); finishReveals();\r\n        clearTimeout(rotateTimer);\r\n        state.hovered = null; highlight(state.selected); $('node-tooltip').hidden = true;\r\n        drag = { x: event.clientX, y: event.clientY, panX: state.panX, panY: state.panY,\r\n            yaw: state.orbit.yaw, pitch: state.orbit.pitch, id: event.pointerId,\r\n            lastX: event.clientX, lastY: event.clientY, lastTime: performance.now(), velocity: { yaw: 0, pitch: 0 },\r\n            rotate: constellation() && state.interaction === 'rotate' && !event.shiftKey && event.button === 0 };\r\n        $('stage').focus({ preventScroll: true });\r\n        $('stage').setPointerCapture(event.pointerId); $('stage').classList.add('dragging');\r\n    });\r\n    $('stage').addEventListener('pointermove', event => {\r\n        if (nodeDrag) {\r\n            if (event.pointerId !== nodeDrag.pointerId) return;\r\n            const dx = event.clientX - nodeDrag.x, dy = event.clientY - nodeDrag.y;\r\n            if (Math.hypot(dx, dy) > 5) suppressClick = true;\r\n            if (suppressClick) {\r\n                state.pinned.set(pinKey(nodeDrag.id), S.unproject({ x: nodeDrag.startX + dx / state.zoom,\r\n                    y: nodeDrag.startY + dy / state.zoom, depth: nodeDrag.depth }, state.orbit, state.pivot));\r\n                $('node-tooltip').hidden = true; draw(0);\r\n            }\r\n            return;\r\n        }\r\n        if (!motionQuery.matches && !drag) {\r\n            const rect = $('stage').getBoundingClientRect();\r\n            state.pointerTarget = { x: (event.clientX - rect.left) / rect.width * 2 - 1, y: (event.clientY - rect.top) / rect.height * 2 - 1 };\r\n        }\r\n        if (!drag || event.pointerId !== drag.id) return;\r\n        const dx = event.clientX - drag.x, dy = event.clientY - drag.y;\r\n        if (Math.hypot(dx, dy) > 5) suppressClick = true;\r\n        if (drag.rotate) {\r\n            const now = performance.now(), dt = Math.max(8, now - drag.lastTime);\r\n            const clampSpeed = value => Math.max(-.002, Math.min(.002, value));\r\n            drag.velocity.yaw = clampSpeed((event.clientX - drag.lastX) * .006 / dt * .55 + drag.velocity.yaw * .45);\r\n            drag.velocity.pitch = clampSpeed(-(event.clientY - drag.lastY) * .006 / dt * .55 + drag.velocity.pitch * .45);\r\n            drag.lastX = event.clientX; drag.lastY = event.clientY; drag.lastTime = now;\r\n            state.orbit.yaw = wrapAngle(drag.yaw + dx * .006);\r\n            state.orbit.pitch = wrapAngle(drag.pitch - dy * .006);\r\n            draw(0);\r\n        } else {\r\n            state.panX = drag.panX + dx; state.panY = drag.panY + dy; applyCamera();\r\n        }\r\n    });\r\n    function endDrag(event) {\r\n        if (event.pointerId !== (nodeDrag?.pointerId ?? drag?.id)) return;\r\n        if (event.type === 'pointerup' && drag?.rotate && suppressClick && motionEnabled() && performance.now() - drag.lastTime \u003c 220) {\r\n            state.inertia = { ...drag.velocity };\r\n        }\r\n        drag = null; nodeDrag = null; $('stage').classList.remove('dragging');\r\n        returnToPanAfterIdle();\r\n        setTimeout(() => { suppressClick = false; }, 0);\r\n    }\r\n    $('stage').addEventListener('pointerup', endDrag); $('stage').addEventListener('pointercancel', endDrag);\r\n    $('stage').addEventListener('lostpointercapture', endDrag);\r\n    $('stage').addEventListener('pointerleave', () => { state.pointerTarget = { x: 0, y: 0 }; });\r\n    $('stage').addEventListener('keydown', event => {\r\n        if (event.target !== $('stage') || !['ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown'].includes(event.key)) return;\r\n        event.preventDefault(); stopChoreography(); finishReveals();\r\n        const dx = event.key === 'ArrowRight' ? 1 : event.key === 'ArrowLeft' ? -1 : 0;\r\n        const dy = event.key === 'ArrowDown' ? 1 : event.key === 'ArrowUp' ? -1 : 0;\r\n        if (constellation() && state.interaction === 'rotate' && !event.shiftKey) {\r\n            clearTimeout(rotateTimer); rotationKeys.add(event.key);\r\n            state.orbit.yaw = wrapAngle(state.orbit.yaw + dx * Math.PI / 18);\r\n            state.orbit.pitch = wrapAngle(state.orbit.pitch - dy * Math.PI / 18);\r\n            draw(0);\r\n        } else {\r\n            state.panX += dx * 40; state.panY += dy * 40; applyCamera();\r\n        }\r\n    });\r\n    document.addEventListener('keyup', event => {\r\n        if (rotationKeys.delete(event.key)) returnToPanAfterIdle();\r\n    });\r\n    function releaseRotationKeys() {\r\n        if (!rotationKeys.size) return;\r\n        rotationKeys.clear(); returnToPanAfterIdle();\r\n    }\r\n    $('stage').addEventListener('blur', releaseRotationKeys);\r\n    window.addEventListener('blur', releaseRotationKeys);\r\n    $('search').addEventListener('input', () => search());\r\n    $('clear-search').addEventListener('click', () => { clearSearch(); $('search').focus(); });\r\n    $('search').addEventListener('keydown', event => {\r\n        if (event.key === 'ArrowDown' || event.key === 'Enter') { $('search-results').querySelector('button')?.focus(); event.preventDefault(); }\r\n        if (event.key === 'Escape') { clearSearch(); event.preventDefault(); }\r\n    });\r\n    document.addEventListener('pointerdown', event => { if (!event.target.closest('.search-wrap')) $('search-results').hidden = true; });\r\n    document.addEventListener('keydown', event => {\r\n        if (event.key === '/' && !$('details-dialog').open && !['INPUT', 'TEXTAREA'].includes(document.activeElement.tagName)) { event.preventDefault(); $('search').focus(); }\r\n        if (event.key === 'Escape' && !$('details-dialog').open) $('search-results').hidden = true;\r\n    });\r\n    $('details-dialog').addEventListener('click', event => {\r\n        if (event.target === $('details-dialog')) {\r\n            const rect = $('details-dialog').getBoundingClientRect();\r\n            if (event.clientX \u003c rect.left || event.clientX > rect.right || event.clientY \u003c rect.top || event.clientY > rect.bottom) navigateBack();\r\n        }\r\n    });\r\n    $('details-dialog').addEventListener('cancel', event => { event.preventDefault(); navigateBack(); });\r\n    $('details-dialog').addEventListener('close', () => { if (previousFocus?.isConnected) previousFocus.focus(); });\r\n    document.addEventListener('visibilitychange', () => {\r\n        document.body.classList.toggle('page-hidden', document.hidden);\r\n        for (const animation of state.animations) document.hidden ? animation.pause() : animation.play();\r\n        schedule();\r\n    });\r\n    motionQuery.addEventListener('change', () => {\r\n        if (motionQuery.matches) { stopChoreography(); finishReveals(); }\r\n        schedule(); draw(0);\r\n    });\r\n    new ResizeObserver(() => {\r\n        if (state.transition) {\r\n            const camera = fittedCamera(state.transition.orbit);\r\n            if (camera) state.transition.camera = camera;\r\n        } else fitGraph();\r\n    }).observe($('stage'));\r\n    function entityDetails(id) {\r\n        const node = state.index.nodes.get(id);\r\n        if (!node) throw new Error(`Unknown node: ${id}`);\r\n        // Copies preserve supplied metadata without giving callbacks access to renderer state.\r\n        return structuredClone({ schemaVersion: state.graph.schemaVersion, repository: state.graph.repository, node,\r\n            parent: node.parentId ? state.index.nodes.get(node.parentId) : null, children: childNodes(id),\r\n            relationships: state.index.adjacency.get(id).map(edge => ({ edge,\r\n                direction: edge.source === id && edge.target === id ? 'self' : edge.source === id ? 'outgoing' : 'incoming',\r\n                node: state.index.nodes.get(edge.source === id ? edge.target : edge.source) })) });\r\n    }\r\n    const startup = globalThis.CodeAtlasStartup.start({\r\n        motionQuery,\r\n        initialize() {\r\n            loadGraph(JSON.parse($('atlas-data').textContent));\r\n            const options = JSON.parse($('atlas-options').textContent);\r\n            Changes.validateOptions(options);\r\n            setTheme(options.theme);\r\n            state.highlightChanges = options.highlightChanges;\r\n            setChangedFiles(options.changedFiles);\r\n        },\r\n        viewport: () => ({ width: $('stage').clientWidth, height: $('stage').clientHeight }),\r\n        settle() { fitGraph(); draw(0); },\r\n        onReady() { state.starting = false; state.last = 0; schedule(); },\r\n        onError(error) { toast(error.message, true); }\r\n    });\r\n    globalThis.CodeAtlas = Object.freeze({\r\n        ready: startup.ready,\r\n        getDetails: entityDetails,\r\n        setChangedFiles, setHighlightChanges, getChangeSummary,\r\n        setTheme, getTheme,\r\n        focusNode(id) { if (!state.index.nodes.has(id)) throw new Error(`Unknown node: ${id}`); focusNode(id); },\r\n        back() { if (!state.navigation.length) return false; navigateBack(); return true; },\r\n        openDetails(id, tab = 'overview') {\r\n            if (!state.index.nodes.has(id)) throw new Error(`Unknown node: ${id}`);\r\n            if (!['overview', 'connections', 'history'].includes(tab)) throw new Error(`Unknown details tab: ${tab}`);\r\n            openDetails(id, tab);\r\n        },\r\n        setOpenDetailsHandler(handler) {\r\n            if (handler !== null && typeof handler !== 'function') throw new TypeError('Details handler must be a function or null.');\r\n            onOpenDetails = handler;\r\n        }\r\n    });\r\n})();\r\n\n(function () {\r\n    'use strict';\r\n    if (parent === window) return;\r\n    // Only the embedding parent can attach a private channel. No broadcast graph data.\r\n    async function connect(event) {\r\n        if (event.source !== parent || event.data?.type !== 'code-atlas:connect' || event.data.version !== 1 || event.ports.length !== 1) return;\r\n        window.removeEventListener('message', connect);\r\n        const port = event.ports[0], atlas = globalThis.CodeAtlas;\r\n        if (!atlas) { port.postMessage({ type: 'failed', error: 'Code Atlas could not initialize.' }); port.close(); return; }\r\n        try { await atlas.ready; }\r\n        catch (error) { port.postMessage({ type: 'failed', error: error.message }); port.close(); return; }\r\n        if (event.data.externalDetails) atlas.setOpenDetailsHandler(details => port.postMessage({ type: 'details', details }));\r\n        port.onmessage = ({ data }) => {\r\n            if (data?.type !== 'command' || !Number.isSafeInteger(data.id)) return;\r\n            try {\r\n                let result;\r\n                switch (data.method) {\r\n                    case 'focusNode': result = atlas.focusNode(data.nodeId); break;\r\n                    case 'getDetails': result = atlas.getDetails(data.nodeId); break;\r\n                    case 'openDetails': result = atlas.openDetails(data.nodeId, data.tab); break;\r\n                    case 'setChangedFiles': result = atlas.setChangedFiles(data.files); break;\r\n                    case 'setHighlightChanges': result = atlas.setHighlightChanges(data.enabled); break;\r\n                    case 'setTheme': result = atlas.setTheme(data.theme); break;\r\n                    case 'getTheme': result = atlas.getTheme(); break;\r\n                    case 'getChangeSummary': result = atlas.getChangeSummary(); break;\r\n                    case 'back': result = atlas.back(); break;\r\n                    default: throw new Error('Unknown Code Atlas command.');\r\n                }\r\n                port.postMessage({ type: 'result', id: data.id, result });\r\n            } catch (error) { port.postMessage({ type: 'result', id: data.id, error: error.message }); }\r\n        };\r\n        port.postMessage({ type: 'ready', version: 1 });\r\n    }\r\n    window.addEventListener('message', connect);\r\n})();\r\n\u003c/script>\r\n\u003c/body>\r\n\u003c/html>\r\n";
const libraries = (() => { const globalThis = {};
(function (root) {
    'use strict';
    const MAX_NODES = 3000;
    const MAX_EDGES = 15000;
    const MAX_BYTES = 8 * 1024 * 1024;
    function assert(ok, message) { if (!ok) throw new Error(message); }
    function object(value, label) {
        assert(value && typeof value === 'object' && !Array.isArray(value), `${label} must be an object.`);
    }
    function string(value, label, max = 20000) {
        assert(typeof value === 'string' && value.trim().length > 0 && value.length <= max,
            `${label} must be a non-empty string (up to ${max} characters).`);
    }
    function optionalStrings(value, fields, label) {
        for (const field of fields) if (value[field] !== undefined) string(value[field], `${label}.${field}`);
    }
    function safeUrl(value) {
        try { const url = new URL(value); return ['https:', 'http:'].includes(url.protocol) ? url.href : null; }
        catch { return null; }
    }
    function validate(graph) {
        object(graph, 'Graph');
        assert(graph.schemaVersion === '1.0', 'schemaVersion must be "1.0".');
        object(graph.repository, 'repository');
        string(graph.repository.name, 'repository.name', 200);
        optionalStrings(graph.repository, ['description', 'branch', 'revision', 'generatedAt'], 'repository');
        if (graph.repository.sample !== undefined) assert(typeof graph.repository.sample === 'boolean', 'repository.sample must be boolean.');
        assert(Array.isArray(graph.nodes) && graph.nodes.length <= MAX_NODES, `nodes must be an array of at most ${MAX_NODES} items.`);
        assert(Array.isArray(graph.edges) && graph.edges.length <= MAX_EDGES, `edges must be an array of at most ${MAX_EDGES} items.`);
        const ids = new Map();
        for (const node of graph.nodes) {
            object(node, 'Node');
            for (const field of ['id', 'name', 'kind']) string(node[field], `Node.${field}`, 500);
            assert(!ids.has(node.id), `Duplicate node id: ${node.id}`);
            ids.set(node.id, node);
            optionalStrings(node, ['parentId', 'language', 'path', 'summary', 'signature'], node.id);
            for (const field of ['metrics', 'changes']) {
                if (node[field] === undefined) continue;
                object(node[field], `${node.id}.${field}`);
                for (const [key, value] of Object.entries(node[field])) {
                    assert(typeof value === 'number' && Number.isFinite(value) && value >= 0,
                        `${node.id}.${field}.${key} must be a finite non-negative number.`);
                    if (key === 'coverage') assert(value <= 100, `${node.id} coverage must be 0–100.`);
                }
            }
            if (node.history !== undefined) {
                assert(Array.isArray(node.history) && node.history.length <= 100, `${node.id}.history must contain at most 100 entries.`);
                for (const h of node.history) {
                    object(h, `${node.id}.history entry`);
                    for (const key of ['commit', 'message', 'author', 'date']) string(h[key], `history.${key}`);
                    assert(!Number.isNaN(Date.parse(h.date)), `Invalid history date for ${node.id}.`);
                    if (h.url !== undefined) assert(safeUrl(h.url), 'History URLs must be absolute HTTP(S) URLs.');
                }
            }
            if (node.links !== undefined) {
                assert(Array.isArray(node.links) && node.links.length <= 20, `${node.id}.links must contain at most 20 entries.`);
                for (const link of node.links) {
                    object(link, 'Link'); string(link.label, 'Link.label', 200);
                    assert(safeUrl(link.url), 'Link URLs must be absolute HTTP(S) URLs.');
                }
            }
        }
        for (const node of graph.nodes) {
            const seen = new Set([node.id]);
            let parent = node.parentId;
            while (parent !== undefined) {
                assert(ids.has(parent), `Missing parent ${parent} for ${node.id}.`);
                assert(!seen.has(parent), `Parent cycle involving ${node.id}.`);
                seen.add(parent); parent = ids.get(parent).parentId;
                assert(seen.size <= 64, 'Hierarchy depth must not exceed 64.');
            }
        }
        const edgeIds = new Set();
        for (const edge of graph.edges) {
            object(edge, 'Edge');
            for (const key of ['id', 'source', 'target', 'kind']) string(edge[key], `Edge.${key}`, 500);
            assert(!edgeIds.has(edge.id), `Duplicate edge id: ${edge.id}`);
            edgeIds.add(edge.id);
            assert(ids.has(edge.source) && ids.has(edge.target), `Edge ${edge.id} references a missing node.`);
        }
        return graph;
    }
    function index(graph) {
        validate(graph);
        const nodes = new Map(graph.nodes.map(n => [n.id, n]));
        const children = new Map([[null, []]]);
        const adjacency = new Map(graph.nodes.map(n => [n.id, []]));
        for (const node of graph.nodes) {
            const parent = node.parentId ?? null;
            if (!children.has(parent)) children.set(parent, []);
            children.get(parent).push(node);
        }
        for (const edge of graph.edges) {
            adjacency.get(edge.source).push(edge);
            if (edge.source !== edge.target) adjacency.get(edge.target).push(edge);
        }
        return { nodes, children, adjacency };
    }
    // Project symbol-level edges onto visible ancestors at the current hierarchy level.
    function aggregateEdges(graph, visibleIds, nodes) {
        const visible = new Set(visibleIds);
        const owners = new Map();
        function owner(id) {
            if (owners.has(id)) return owners.get(id);
            let current = id;
            while (current && !visible.has(current)) current = nodes.get(current)?.parentId;
            owners.set(id, current); return current;
        }
        const result = new Map();
        for (const edge of graph.edges) {
            const source = owner(edge.source), target = owner(edge.target);
            if (!source || !target || (source === target && edge.source !== edge.target)) continue;
            const key = JSON.stringify([source, target, edge.kind]);
            if (!result.has(key)) result.set(key, { source, target, kind: edge.kind, count: 0 });
            result.get(key).count++;
        }
        return [...result.values()];
    }
    // JSON sits in a raw-text script element. Escape '<' so data cannot terminate it.
    function serialize(graph) {
        return JSON.stringify(graph).replace(/</g, '\\u003c').replace(/\u2028/g, '\\u2028').replace(/\u2029/g, '\\u2029');
    }
    root.CodeAtlasContract = Object.freeze({ validate, index, aggregateEdges, serialize, safeUrl, MAX_NODES, MAX_EDGES, MAX_BYTES });
})(globalThis);

(function (root) {
    'use strict';
    const MAX_FILES = 10000;
    // Repository-relative, case-sensitive paths. Never match just a basename or a prefix.
    function normalizePath(value, location = false) {
        if (typeof value !== 'string' || !value.trim() || value.length > 4096 || /[\u0000-\u001f\u007f]/.test(value)) return null;
        let path = value.replace(/\\/g, '/');
        if (location) path = path.replace(/:\d+(?::\d+)?$/, '');
        if (path.startsWith('/') || /^[a-z][a-z\d+.-]*:/i.test(path) || path.endsWith('/')) return null;
        const parts = path.split('/').filter(part => part && part !== '.');
        if (!parts.length || parts.includes('..')) return null;
        const normalized = parts.join('/');
        return /^[a-z][a-z\d+.-]*:/i.test(normalized) ? null : normalized;
    }
    function validateOptions({ changedFiles = [], highlightChanges = true } = {}) {
        if (!Array.isArray(changedFiles) || changedFiles.length > MAX_FILES) throw new TypeError(`changedFiles must be an array of at most ${MAX_FILES} repository-relative file paths.`);
        if (typeof highlightChanges !== 'boolean') throw new TypeError('highlightChanges must be a boolean.');
        const paths = new Set();
        for (const value of changedFiles) {
            const path = normalizePath(value);
            if (!path) throw new TypeError('Each changedFiles entry must be a repository-relative file path (1–4096 characters), without a drive, URL, control characters or .. segments.');
            paths.add(path);
        }
        return { changedFiles: [...paths], highlightChanges };
    }
    function match(index, files) {
        const { changedFiles } = validateOptions({ changedFiles: files ?? null });
        const wanted = new Set(changedFiles), found = new Set(), paths = new Map(), nodeIds = [];
        function pathFor(node) {
            if (paths.has(node.id)) return paths.get(node.id);
            // A supplied path is authoritative, even if unsupported. Only missing paths inherit.
            const path = node.path !== undefined ? normalizePath(node.path, true)
                : node.parentId ? pathFor(index.nodes.get(node.parentId)) : null;
            paths.set(node.id, path); return path;
        }
        for (const node of index.nodes.values()) {
            const path = pathFor(node);
            if (wanted.has(path)) { found.add(path); nodeIds.push(node.id); }
        }
        return { changedFiles, matchedFiles: changedFiles.filter(path => found.has(path)),
            unmatchedFiles: changedFiles.filter(path => !found.has(path)), nodeIds };
    }
    root.CodeAtlasChanges = Object.freeze({ normalizePath, validateOptions, match });
})(globalThis);

(function (root) {
    'use strict';
    // Match VibeRails' semantic CSS variable names; graph-specific tokens stay separate.
    const tokens = {
        colors: { background: '--color-bg-base', surface: '--color-bg-surface', surfaceHover: '--color-bg-surface-hover',
            elevated: '--color-bg-elevated', primary: '--color-primary', primaryHover: '--color-primary-dark',
            onPrimary: '--color-on-primary', accent: '--color-accent', text: '--color-text', muted: '--color-text-muted',
            border: '--color-border', success: '--color-success', warning: '--color-warning', danger: '--color-danger', shadow: '--color-shadow' },
        nodes: { module: '--node-module', class: '--node-class', file: '--node-file', function: '--node-function', data: '--node-data' },
        graph: { edge: '--graph-edge', crossEdge: '--graph-cross-edge', changed: '--graph-changed' }
    };
    const palettes = {
        dark: {
            colors: { background: '#101319', surface: '#171c26', surfaceHover: '#263247', elevated: '#202733',
                primary: '#a6b7ed', primaryHover: '#bac8f3', onPrimary: '#172238', accent: '#78dce8', text: '#e3e8ef',
                muted: '#99a5b8', border: '#2d3543', success: '#46c9b0', warning: '#f2bd72', danger: '#ed939e', shadow: '#000000' },
            nodes: { module: '#9b8afb', class: '#a9b8ff', file: '#62b0f5', function: '#46c9b0', data: '#78dce8' },
            graph: { edge: '#93a4be', crossEdge: '#b2a5d4', changed: '#b5ecf4' }
        },
        light: {
            colors: { background: '#f4f7fc', surface: '#ffffff', surfaceHover: '#dfe8f5', elevated: '#e8eef7',
                primary: '#395bc3', primaryHover: '#2b469c', onPrimary: '#ffffff', accent: '#08798b', text: '#1b2940',
                muted: '#54647d', border: '#c6d2e3', success: '#14755d', warning: '#946100', danger: '#c03342', shadow: '#000000' },
            nodes: { module: '#7454c4', class: '#455fc5', file: '#2770b0', function: '#18816f', data: '#167a8a' },
            graph: { edge: '#61748e', crossEdge: '#8872a4', changed: '#0b7d91' }
        }
    };
    function record(value, name, allowed) {
        if (!value || typeof value !== 'object' || Array.isArray(value)) throw new TypeError(`${name} must be an object.`);
        for (const key of Object.keys(value)) if (!allowed.includes(key)) throw new TypeError(`Unknown ${name} option: ${key}`);
    }
    function color(value, name) {
        if (typeof value !== 'string' || !/^#(?:[\da-f]{3}|[\da-f]{4}|[\da-f]{6}|[\da-f]{8})$/i.test(value)) {
            throw new TypeError(`${name} must be a hex color (#RGB, #RGBA, #RRGGBB or #RRGGBBAA).`);
        }
        const digits = value.slice(1).toLowerCase();
        return '#' + (digits.length <= 4 ? [...digits].map(c => c + c).join('') : digits);
    }
    function resolve(theme = {}) {
        record(theme, 'theme', ['mode', ...Object.keys(tokens)]);
        const mode = theme.mode === undefined ? 'dark' : theme.mode;
        if (!['dark', 'light'].includes(mode)) throw new TypeError('theme.mode must be dark or light.');
        const result = { mode };
        for (const [group, properties] of Object.entries(tokens)) {
            const overrides = theme[group] === undefined ? {} : theme[group];
            record(overrides, `theme.${group}`, Object.keys(properties));
            result[group] = { ...palettes[mode][group] };
            for (const key of Object.keys(overrides)) result[group][key] = color(overrides[key], `theme.${group}.${key}`);
        }
        return result;
    }
    function toCss(theme) {
        const resolved = resolve(theme);
        const declarations = [`color-scheme:${resolved.mode}`];
        for (const [group, properties] of Object.entries(tokens)) {
            for (const [key, variable] of Object.entries(properties)) declarations.push(`${variable}:${resolved[group][key]}`);
        }
        return `:root{${declarations.join(';')}}`;
    }
    for (const group of Object.values(tokens)) Object.freeze(group);
    root.CodeAtlasTheme = Object.freeze({ resolve, toCss, color, tokens: Object.freeze(tokens) });
})(globalThis);

(function (root) {
    'use strict';
    const MAX_LOGO_BYTES = 512 * 1024;
    const escape = text => String(text).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]);
    function imageType(bytes) {
        const starts = values => values.every((value, i) => bytes[i] === value);
        const text = (start, end) => String.fromCharCode(...bytes.slice(start, end));
        return starts([137, 80, 78, 71, 13, 10, 26, 10]) ? 'image/png'
            : starts([255, 216, 255]) ? 'image/jpeg'
            : ['GIF87a', 'GIF89a'].includes(text(0, 6)) ? 'image/gif'
            : text(0, 4) === 'RIFF' && text(8, 12) === 'WEBP' ? 'image/webp' : null;
    }
    function validateBranding({ title, logo } = {}) {
        if (title !== undefined && (typeof title !== 'string' || !title.trim() || title.length > 200)) throw new Error('title must be a nonempty string of at most 200 characters.');
        if (logo !== undefined) {
            const match = typeof logo === 'string' && /^data:(image\/(?:png|jpeg|webp|gif));base64,([A-Za-z0-9+/]+={0,2})$/.exec(logo);
            if (!match || match[2].length > Math.ceil(MAX_LOGO_BYTES / 3) * 4) throw new Error('logo must be a PNG, JPEG, WebP or GIF base64 data URL of at most 512 KiB.');
            let raw;
            try { raw = atob(match[2]); } catch { throw new Error('Invalid base64 logo.'); }
            if (btoa(raw) !== match[2]) throw new Error('Invalid base64 logo.');
            const bytes = Uint8Array.from(raw, c => c.charCodeAt(0));
            if (bytes.length > MAX_LOGO_BYTES || imageType(bytes) !== match[1]) throw new Error('logo image bytes do not match a supported image type or size.');
        }
        return { title, logo };
    }
    function fillTemplate(template, graph, json, options = {}) {
        const { title, logo } = validateBranding(options);
        const changes = root.CodeAtlasChanges.validateOptions(options);
        const theme = root.CodeAtlasTheme.resolve(options.theme);
        const branding = title || logo ? `<div id="consumer-brand" class="consumer-brand">${logo ? `<img src="${logo}" alt="${title ? '' : 'Project logo'}" width="32" height="32">` : ''}${title ? `<span title="${escape(title)}">${escape(title)}</span>` : ''}</div>` : '';
        const values = { TITLE: escape(title ?? `${graph.repository.name} · Code Atlas`), BRANDING: branding, DATA: json, THEME: root.CodeAtlasTheme.toCss(theme), MODE: theme.mode, OPTIONS: JSON.stringify({ ...changes, theme }).replace(/</g, "\\u003c") };
        return template.replace(/<!-- ATLAS_(TITLE|DATA|BRANDING|OPTIONS|THEME|MODE) -->/g, (_, key) => values[key]);
    }
    root.CodeAtlasPresentation = Object.freeze({ imageType, validateBranding, fillTemplate, MAX_LOGO_BYTES });
})(globalThis);

return globalThis; })();
// Bundled by build-browser.mjs. Read host CSS explicitly: iframe styles never leak across instances.
function createThemeFromCss(library) {
    return function themeFromCss(element = document.documentElement, { mode } = {}) {
        if (!(element instanceof Element) || element.ownerDocument !== document || !element.isConnected) throw new TypeError('themeFromCss requires a connected element in this page.');
        const styles = getComputedStyle(element), theme = {};
        const canvas = document.createElement('canvas'); canvas.width = canvas.height = 1;
        const context = canvas.getContext('2d', { willReadFrequently: true });
        const probe = document.createElement('span'); probe.style.cssText = 'display:none!important';
        element.append(probe);
        try {
            for (const [group, properties] of Object.entries(library.tokens)) {
                for (const [key, variable] of Object.entries(properties)) {
                    const value = styles.getPropertyValue(variable).trim();
                    if (!value) continue;
                    if (!CSS.supports('color', value)) throw new TypeError(`${variable} does not contain a supported CSS color.`);
                    probe.style.color = value;
                    context.clearRect(0, 0, 1, 1);
                    context.fillStyle = getComputedStyle(probe).color;
                    context.fillRect(0, 0, 1, 1);
                    const rgba = [...context.getImageData(0, 0, 1, 1).data];
                    if (rgba[3] === 255) rgba.pop();
                    (theme[group] ??= {})[key] = '#' + rgba.map(n => n.toString(16).padStart(2, '0')).join('');
                }
            }
        } finally { probe.remove(); }
        const scheme = styles.colorScheme;
        const rgb = theme.colors?.background?.slice(1, 7).match(/../g)?.map(value => parseInt(value, 16));
        theme.mode = mode ?? (['dark', 'light'].includes(scheme) ? scheme
            : rgb && (rgb[0] * .299 + rgb[1] * .587 + rgb[2] * .114) > 150 ? 'light' : 'dark');
        library.resolve(theme); // Validate explicit mode as well as normalized color values.
        return theme;
    };
}

export const themeFromCss = createThemeFromCss(libraries.CodeAtlasTheme);
// This factory is bundled with trusted renderer assets by build-browser.mjs.
function createMount(template, contract, presentation) {
    const mounted = new WeakMap();
    return function mountCodeAtlas(container, { graph, title, logo, changedFiles, highlightChanges, theme, cspNonce, onOpenDetails, onError } = {}) {
        if (!(container instanceof HTMLElement) || container.ownerDocument !== document || !container.isConnected) throw new TypeError('container must be a connected HTML element in this page.');
        if (mounted.has(container)) throw new Error('This container already has a Code Atlas instance. Destroy it before remounting.');
        for (const [name, fn] of Object.entries({ onOpenDetails, onError })) {
            if (fn !== undefined && typeof fn !== 'function') throw new TypeError(`${name} must be a function.`);
        }
        contract.validate(graph);
        const json = contract.serialize(graph);
        if (new TextEncoder().encode(json).length > contract.MAX_BYTES) throw new Error('Graph exceeds the 8 MiB limit.');
        // Nonce only the trusted renderer template, before any graph or branding data enters it.
        // The child keeps its opaque origin and its own restrictive CSP. This nonce satisfies
        // the host's inherited script policy; it does not grant any new host capability.
        if (cspNonce !== undefined && (typeof cspNonce !== 'string' || !/^[A-Za-z0-9+/_-]{1,128}={0,2}$/.test(cspNonce))) throw new TypeError('cspNonce must be a nonempty base64-compatible nonce of at most 130 characters.');
        const trustedTemplate = cspNonce === undefined ? template : template.replace(/<script(?=[\s>])/gi, () => `<script nonce="${cspNonce}"`);
        const html = presentation.fillTemplate(trustedTemplate, graph, json, { title, logo, changedFiles, highlightChanges, theme });
        const frame = document.createElement('iframe');
        frame.title = title ?? `${graph.repository.name} code graph`;
        // A separate document isolates styles, DOM IDs, shortcuts and animation lifetimes.
        frame.setAttribute('sandbox', 'allow-scripts allow-popups allow-popups-to-escape-sandbox');
        frame.style.cssText = 'display:block;width:100%;height:100%;border:0;';
        frame.srcdoc = html;
        const channel = new MessageChannel(), pending = new Map();
        let destroyed = false, connected = false, sequence = 0, resolveReady, rejectReady;
        const ready = new Promise((resolve, reject) => { resolveReady = resolve; rejectReady = reject; });
        // Hosts may destroy immediately without waiting; keep that rejected ready promise handled.
        ready.catch(() => {});
        function reportFailure(error) {
            if (onError) {
                try { onError(error); } catch (handlerError) { console.error('Code Atlas onError failed:', handlerError); }
            } else console.error('Code Atlas:', error);
        }
        function report(error) { if (!destroyed) reportFailure(error instanceof Error ? error : new Error(String(error))); }
        function fail(error) {
            if (destroyed) return;
            destroy(error); reportFailure(error);
        }
        const startupTimer = setTimeout(() => fail(new Error('Code Atlas did not become ready within 15 seconds. Check the host CSP and container.')), 15000);
        function destroy(reason = new Error('Code Atlas instance was destroyed.')) {
            if (destroyed) return;
            destroyed = true; clearTimeout(startupTimer);
            frame.removeEventListener('load', connect);
            channel.port1.close(); channel.port2.close();
            for (const entry of pending.values()) { clearTimeout(entry.timer); entry.reject(reason); }
            pending.clear(); rejectReady(reason); frame.remove(); mounted.delete(container);
        }
        function connect() {
            if (destroyed) return;
            if (connected) { fail(new Error('The Code Atlas frame was unexpectedly reloaded. Remount it.')); return; }
            connected = true;
            // The sandbox gives srcdoc an opaque origin; the transferred port authenticates replies.
            try { frame.contentWindow.postMessage({ type: 'code-atlas:connect', version: 1, externalDetails: !!onOpenDetails }, '*', [channel.port2]); }
            catch (error) { fail(error); }
        }
        channel.port1.onmessage = ({ data }) => {
            if (destroyed) return;
            if (data?.type === 'ready' && data.version === 1) { clearTimeout(startupTimer); resolveReady(); }
            else if (data?.type === 'failed') fail(new Error(data.error));
            else if (data?.type === 'result') {
                const entry = pending.get(data.id); if (!entry) return;
                pending.delete(data.id); clearTimeout(entry.timer);
                data.error ? entry.reject(new Error(data.error)) : entry.resolve(data.result);
            } else if (data?.type === 'details' && onOpenDetails) {
                try { Promise.resolve(onOpenDetails(data.details, controller)).catch(report); }
                catch (error) { report(error); }
            }
        };
        async function command(method, args = {}) {
            const payload = structuredClone(args);
            const { nodeId, tab } = payload;
            if (destroyed) throw new Error('Code Atlas instance was destroyed.');
            if (['focusNode', 'getDetails', 'openDetails'].includes(method) && (typeof nodeId !== 'string' || !nodeId.trim())) throw new TypeError('Node ID must be a nonempty string.');
            if (method === 'openDetails' && !['overview', 'connections', 'history'].includes(tab)) throw new Error(`Unknown details tab: ${String(tab)}`);
            await ready;
            if (destroyed) throw new Error('Code Atlas instance was destroyed.');
            return new Promise((resolve, reject) => {
                const id = ++sequence;
                const timer = setTimeout(() => { pending.delete(id); reject(new Error(`Code Atlas ${method} timed out.`)); }, 15000);
                pending.set(id, { resolve, reject, timer });
                try { channel.port1.postMessage({ type: 'command', id, method, ...payload }); }
                catch (error) { pending.delete(id); clearTimeout(timer); reject(error); }
            });
        }
        const controller = Object.freeze({ ready,
            focusNode: id => command('focusNode', { nodeId: id }),
            getDetails: id => command('getDetails', { nodeId: id }),
            openDetails: (id, tab = 'overview') => command('openDetails', { nodeId: id, tab }),
            back: () => command('back'),
            setChangedFiles: files => command('setChangedFiles', { files }),
            setHighlightChanges: enabled => command('setHighlightChanges', { enabled }),
            getChangeSummary: () => command('getChangeSummary'),
            setTheme: (theme = {}) => command('setTheme', { theme }),
            getTheme: () => command('getTheme'),
            destroy: () => destroy()
        });
        frame.addEventListener('load', connect);
        mounted.set(container, controller); container.append(frame);
        return controller;
    };
}

export const mountCodeAtlas = createMount(rendererTemplate, libraries.CodeAtlasContract, libraries.CodeAtlasPresentation);
